# Fix stack tracker

Ephemeral coordination doc between Lex and the operator for the
current Fix 1-13+ work cycle. Purged once the whole stack ships and
the smoke test passes. The git history is the durable record; this
file is for live status only.

Status legend: ⏳ queued, 🔄 in progress, ✅ shipped, ❌ blocked.

## 2026-07-18 voice top-layer smarts (VOICE-TOP-LAYER-SMARTS-SPEC, tests-first, additive-only)

Spec `C:\dev\data\skill-connections\brainstorm\VOICE-TOP-LAYER-SMARTS-SPEC.md`. Daemon NOT restarted (operator-only); rebuild required before live verify.

| # | Title | Status | Commit | Evidence + fix |
|---|-------|--------|--------|-----------------|
| VS-P0 | Silent drop at the top layer: an ack reached the surface and was never spoken or logged | ✅ shipped, awaiting rebuild + live verify | 8ef3c8d | **Evidence (spec P0):** live session, a pre-tool ack reached the speak path and produced no audio and no log line - `clampAck` fell back to the canned `'On it.'` sentinel, which the no-hardcoded-talking rule refuses to speak, and the caller's `if (ack !== 'On it.')` then did NOTHING: no `speak()`, no log. A silent drop, undiagnosable. **Fix (additive):** two TOTAL pure decisions in `select-tts-content.ts` - `decidePreToolAck(text)` yields either `speak` (the clamped ack) or a NAMED `dropReason` (`ack-clamped-to-canned`), and generic `accountSpeech(text)` yields `speak` (trimmed) or a named `null`/`empty` drop. Each returns exactly one of speak / dropReason - never both, never neither (the invariant: no silent nothing). The WS pre-tool-ack site now speaks `decision.speak` or logs `[voice-ws] TOP-LAYER ACK DROPPED reason=<named>`. **Tests:** 7 new pins in `tests/select-tts-content.test.ts` incl. a totality property over 8 sample inputs for each decision. Suite green, tsc clean. **Rebuild:** yes (daemon). |
| VS-P1 | Ack was DEEP-sourced (clampAck of Lex's first sentence echoed up late) = stale-ack race + double-ack with the top's own handoff | ✅ shipped, awaiting rebuild + live verify | 4c05afd | **Spec P1:** move ack ownership to the TOP (haiku) layer. The top's spoken handoff line IS the ack (it streams instantly, at the top's latency); the deep layer stops emitting pre-tool acks once the top owns it. **Fix (additive):** two pure module-level decisions in `lex-voice-ws.ts` - `_topOwnsAckAfterForwardImpl({earlySpokenCount, remainderSpeech})` is true iff the top actually spoke a handoff for the forward (streamed lines and/or a final remainder), and `_shouldSpeakDeepAckImpl({topOwnsAck})` suppresses the deep pre-tool ack (`top-owns-ack`) or allows it as the fallback (`no-top-ack-fallback`). New `ConnState.topOwnsAck` is set from the first fn on every real forward and cleared at the deep turn's end_turn; the pre-tool-ack site gates on the second fn. Result: a normal escalated turn yields exactly ONE ack (the top's), never two; a fail-safe forward (session down, no top speech) still gets the deep ack so the operator always hears one. **Tests:** 8 new pins in `tests/voice-top-ack-ownership.test.ts` (owns-ack derivation incl. fail-safe, suppress-vs-fallback, and two composed one-ack-never-two proofs). tsc clean. **Rebuild:** yes (daemon). |
| VS-P2 | Trivial turns (greetings, chit-chat, thanks, acks) could be escalated to the deep PTY instead of being fielded at the top | ✅ shipped, awaiting rebuild + live verify | (this commit) | **Spec P2:** trivial conversational turns are answered by the TOP layer directly and NEVER escalated; only substance goes down; when unsure escalate (fail toward substance). **Fix (additive, decision in the top layer):** new strict `isTrivialTopTurn(utterance)` in `voice-top-layer.ts` matches the WHOLE normalized utterance (lowercased, trailing punctuation dropped, a leading/trailing `lex` address token stripped) against greeting / thanks / ack / sign-off / "you there" phrase sets - a substring never matches, so `thanks for breaking the build, what happened` stays substance. `topLayerTurn` strips any FORWARD the model over-emitted on a trivial turn ONLY when the top actually produced speech to answer with (`result.speech !== null || consumed.length > 0`); with no top speech the forward survives and Lex answers (fail toward substance). The top-layer system contract now states the rule explicitly. **Tests:** 6 new pins in `tests/voice-top-layer.test.ts` (trivial-phrase set incl. `lex`-address + punctuation/case, substance-is-false incl. bare yes/no, forward-dropped-on-trivial, forward-kept-on-substance, no-strip-without-speech). tsc clean. **Rebuild:** yes (daemon). |
| VS-P3 | Confirm coalesce is the INTERRUPT case only (not an output ack-vs-answer race), scoped to the mid-reply path | ✅ shipped, awaiting rebuild + live verify | (this commit) | **Spec P3 (partly existed):** coalesce applies ONLY when the operator stacks utterances while a reply is in flight - combine into ONE handling, no double-answer; normal sequential turns unaffected. **Confirmation (audit):** both queue sites already gated on an in-flight flag - top-layer on `state.topTurnInFlight`, direct-llm on `state.inFlightDirectLlmReply`; `formatQueueDrain`/`mergeOperatorUtterances` only run on a drain of a queue populated mid-reply. **Fix (additive, locks the scope):** extracted the scope decision to pure exported `_shouldCoalesceMidReplyImpl({replyInFlight})` and routed BOTH queue sites through it, so a future change cannot silently broaden coalescing to sequential turns. `mergeOperatorUtterances` merge shape (single pass-through vs multi-combine) already pinned in `voice-top-coalesce.test.ts`. **Tests:** 2 new pins (coalesce iff in flight; a sequential turn is never coalesced) on top of the 5 merge pins + `lex-voice-coalesce` contradiction/drain pins. tsc clean. **Rebuild:** yes (daemon). |

All four shipped, committed, daemon NOT yet restarted (per operator instruction). Rebuild + restart required before live verification.

| # | Title | Status | Commits | Evidence + fix |
|---|-------|--------|---------|-----------------|
| SM-1 | Utterance dropped on turn fire (empty prompt to Lex) | ✅ shipped, awaiting live verify | 33deb2d | **Evidence:** daemon.log 03:27:08.998Z shows `top-layer turn speech=null forward="Okay, here's the problem."` with NO queue-push line after it (Lex was idle, so the code took the direct `ptyInject` path). Lex's jsonl (fc7fcba2) has the matching user record at 03:27:09.378Z: exactly 4094 chars, cut MID-SENTENCE inside the live_state snapshot; the tail (`[voice mode] Okay, here's the problem.`) never landed anywhere in the transcript, and Lex replied "Your last turn came through empty on my side, no words in it." **Root cause, reproduced twice against a real interactive claude in a node-pty harness:** a single `pty.write()` of an 8937-char payload landed as a 4841-char user record - exactly 4096 chars dropped (Windows console input event queue overflow on a one-shot multi-KB burst; the raw ConPTY transport passes 9KB fine to a cooked-mode reader, so the loss is specific to claude's raw-mode input loop). The same payload in 2048-char slabs 20ms apart landed complete (8937/8937). The operator's words ride the TAIL of the ~8-9KB inject payload (snapshot first), so the dropped block always eats the utterance. **Fix:** `ptyInject` now splits payloads >2048 chars into 2048-char slabs written 20ms apart; trailing `\r` rides the final slab (Fix 19 paste-close+Enter ordering preserved); 1s bare-CR nudge anchored to the LAST slab; slab write failures logged loudly. Small injects byte-identical. 9/9 regression tests (`tests/pty-inject-payload.test.ts`). |
| SM-2 | Smart-heartbeat generator "crashes" silently (0xC000013A, dead air 4 min) | ✅ shipped, awaiting live verify | f29444e | **Evidence:** pty-host exits 03:25:46.138Z (pty bba2a451) and 03:30:54.465Z (pty 3a67662b), both code -1073741510 with `ptyKill` fired ~1s earlier. Both voice-brain transcripts (6c5bf366, 4504771a) contain exactly ONE user record and ZERO assistant records. **Corrected diagnosis: the process did not die instantly on its own.** Claude boots in 4-20s while asks time out at 3-6s, so the first two asks after every lazy spawn timed out; the two-consecutive-timeouts liveness rule then KILLED the still-booting session (that ptyKill IS the 0xC000013A exit; taskkill /F /T is the kill signature), with the heartbeat prompt as `last_command` only because it was the final inject before the kill. The first ask's pasted text sat uncommitted in the booting composer for 25s (boot overlays ate the CR) until a later ask's CR pushed it through, which is why the one user record carries the wrong ask. The 5-min respawn cooldown then nulled everything, producing the 4-minute dead-air stretch. All of it invisible: `setVoiceBrainSessionLogger` was never wired in daemon.ts. **Fix:** warmup gate (no asks accepted until a boot probe sees a real assistant reply; 45s bound; banner pre-dismiss CRs; idempotent re-nudge CRs; warming asks null fast without injecting and never count as timeouts), respawn cooldown 5min → 60s, voice-brain logger wired (spawn / warm / ask latency / timeout / kill / cooldown all in daemon.log now), heartbeat gets a dedicated 10s ask timeout (was sharing the 3s render bound), and a missed pulse logs `HEARTBEAT MISSED` loudly instead of dying silent. 24/24 lifecycle tests reworked+green. |
| SM-3 | Talk layer never speaks (speech=null every turn) | ✅ shipped, awaiting live verify | f29444e | **Evidence:** every `top-layer turn` line 03:25-03:31Z logs `speech=null` with `forward=` equal to the verbatim utterance - the exact fail-safe signature of `topLayerTurn` (ask returned null/timeout → `{speech:null, forward:utterance}`). Zero assistant records in both voice-brain transcripts confirm the brain never answered a single ask all session. **Verdict: broken generation path (the SM-2 death spiral), not an over-aggressive gate** - the parse/gate layers were never reached. **Fix:** same engine rework as SM-2, plus `prewarmVoiceBrainSession()` fired on voice WS attach so the brain boots when the voice panel connects and the FIRST operator utterance meets a warm brain (real speech + forward handoff acks when Lex goes heads-down) instead of paying boot cost or hitting the spiral. |
| SM-4 | Mid-reply TTS truncation reproduced live (Fix 24 recheck) | ✅ shipped, awaiting live verify | f9a787b | **Old diagnosis re-verified and REJECTED:** the 2026-06-22 cause (haiku `renderReplyLive` max_tokens 512) cannot apply - spec v2 removed that restyle entirely. **Verified live mechanism:** Lex's 1031-char reply (end_turn 03:27:53Z) was playing via the raw fallback path when VAD fired utterance-start on echo/noise at ~03:28:30Z (mic stays hot during playback since the AEC rework). `utterance-start` unconditionally killed the in-flight piper segment AND cleared every queued sentence; whisper transcribed the barging "utterance" as `[BLANK_AUDIO]` (03:28:35.028Z log line), the noise floor dropped it, nothing injected, nothing resumed - the body never finished. duringTts=false on the operator's 03:28:37Z turn confirms TTS was already dead ~44s into an ~65s body. Operator called it out at 03:30:13Z ("You didn't even finish that last statement"). **Fix (words-not-energy barge):** audio still stops instantly on utterance-start, but the interrupted segment + queued sentences are stashed and the PTY Ctrl+C deferred; phantom resolutions (empty / `[BLANK_AUDIO]` / noise floor / judged echo) RESUME the stash (interrupted sentence restarts, partialChain entry popped); real words drop the stash and fire the deferred Ctrl+C, so phantom noise can no longer abort Lex's turn either. **Second hole closed pre-emptively:** `voiceLexReply` latched delivered=true on the first partial and returned true even when the flat 3s deadline cut the stream (a truncation the raw fallback could never catch - it would bite the moment SM-2/3 make the brain answer). Delivery deadline now scales with body length (floor 3s, ~15ms/char, cap 30s); a partial-then-timeout logs `LEX REPLY DELIVERY CUT MID-STREAM` loudly. 12 new regression tests (`tests/voice-barge-resume.test.ts`). |

**Post-restart verification checklist (operator):** (1) speak a normal turn → daemon.log shows `[voice-brain] spawned ... warmup starting` then `[voice-brain] warm: first reply after Nms` on panel open, and `top-layer turn speech="..."` non-null on turns; (2) let Lex work >90s → still-on-it pulse spoken, or `HEARTBEAT MISSED` in the log; (3) speak a long multi-KB-context turn → Lex's user record carries the full `[voice mode] <words>` tail; (4) let a long reply play and stay silent → body plays to the end; cough mid-reply → audio dips then resumes the sentence.

### Second wave (04:20-04:47Z live failures after restart)

| # | Title | Status | Commits | Evidence + fix |
|---|-------|--------|---------|-----------------|
| SM-7 | Dashboard EADDRINUSE crash loop; -Force relauncher hard-killed the healthy successor daemon | ✅ shipped, awaiting live verify (orphan already reclaimed live) | (this commit) | **Evidence:** daemon 104048 (04:20 restart) died at ~04:22:31Z with NO [shutdown] lines; pid 121548 started 04:22:33Z. The killer: start-daemon.ps1 -Force reads daemon.pid AT TASK-RUN TIME - a late/duplicate DevNeural-Daemon-Restart run read the pidfile after the successor had replaced it, watched the healthy 104048 "overstay" 40s, and Stop-Process'd it (single process, not tree). Its next-dev child survived as an orphan holding :3000 (pid 106480, `next/dist/server/lib/start-server.js` under 08-dashboard), and every later daemon's dashboard child died `EADDRINUSE` every 30s - 19+ attempts, 04:22-04:34Z, no detection, no reclaim, no escalation (next exits code=0 after the bind error, so the exit code says nothing). **Live remediation (done during this session, no daemon restart):** verified 106480's command line, taskkill /F /T, supervisor rebound :3000 on pid 120548 within one respawn cycle. **Fix 1 (supervisor):** stderr EADDRINUSE detector per child; on conflict, resolve the port holder (netstat -ano) + its command line (CIM), and when it is OUR dashboard (isOwnDashboardCommandLine: dashboardDir or next/dist under node) tree-kill and respawn in 1s; a FOREIGN holder is never killed - escalating backoff 30s x2^n capped 10min, and after 10 consecutive conflicts the supervisor stops retrying with a PORT PERMANENTLY UNAVAILABLE log (pre-fix: blind 30s loop forever). All probes windowsHide; injectable ops seam for tests. **Fix 2 (relauncher pid race):** /admin/daemon/restart now writes `daemon.restart.expect` (the pid that is SUPPOSED to die) before arming the task; -Force refuses to wait on or kill any pid that does not match it, and with no expect file it is health-gated: a pid answering /health is never touched. Legit overstay kills now use taskkill /F /T (tree) so the dashboard child dies with the daemon instead of orphaning. ps1 parse-checked. **Tests:** 8 new pins (own-orphan reclaim + immediate respawn, foreign never killed + escalating backoff, permanent give-up at cap, failed reclaim falls back, transient no-holder, isOwnDashboardCommandLine shapes). **Known pre-existing suite noise, not from this wave:** tests/grooming-routes.test.ts (documented pre-existing) and tests/sessions-anchor-liveness.test.ts "identity directory missing" (environmental flake: it reads the real %LOCALAPPDATA% stream-deck identity dir, which has FRESH files whenever the operator's VS Code bridge is heartbeating - fails/passes with the live heartbeat, verified via stash bisect at 01:08-01:10Z). |
| SM-6 | Voice->Lex prompt stuck unsubmitted at the terminal (trailing Enter swallowed, zero self-recovery) | ✅ shipped, awaiting live verify | 8f1c111 | **Evidence:** 04:45Z operator screenshot - his message sitting at the Lex terminal as unsubmitted `[Pasted text #66-71]` blocks; per operator correction it NEVER self-recovered, he had to click in, type characters, and press Enter manually. **Root cause 1 (regression from 33deb2d, mine):** the slab path embedded the commit `\r` in the FINAL slab; claude's paste coalescer absorbed it as pasted CONTENT (the six paste blocks are one ~12KB inject in 2048-char slabs) - the exact Fix 19 trailing-Enter drop reintroduced at slab granularity. The harness that validated slab delivery wrote its CR as a SEPARATE delayed write; the shipped code did not match it. **Fix 1:** slabs are now built from the TEXT (never carry a CR); the commit `\r` fires as its own write 250ms after the final slab (`PTY_INJECT_SLAB_SETTLE_MS`, the settle-then-Enter shape seedFirstTurn already uses and the harness verified); the 1s bare-CR nudge anchors past slabs + settle. Single-slab payloads keep the proven Fix 19 atomic body+`\r`. **Root cause 2:** nothing ever verified that a committed inject SUBMITTED - the nudges fired blind and the failure was invisible. **Fix 2:** delivery confirmation on the voice->Lex path (`_verifyInjectDeliveryImpl`): after every idle-time commit inject (direct turn inject + turn-boundary queue flush) the bound session jsonl is scanned for a user record carrying a fingerprint of the utterance (parsed records, so JSON escaping cannot mask a hit); a miss fires an idempotent CR retry at 4s intervals, the final retry escalates to `' \r'` (space+Enter, the manual recovery that provably works), and exhausted retries log `INJECT DELIVERY FAILED` loudly + push an `inject-delivery` error frame to the client. Same class as the bridge VSIX deliverability verdicts from the 2026-07-09 handover; mid-turn steering injects are excluded by construction (their user record only lands at pickup). **Tests:** 7 pins on the verifier (confirm / retry-confirm / exhausted-failure / JSON-escaping / user-records-only / no-jsonl / string-content) + slab-CR contract pin (no slab ever carries `\r`). |
| SM-5 | Watchdog kills brain mid-speech (delivery 04:30Z, heartbeat 04:46Z); warmup killed at 45s under load | ✅ shipped, awaiting live verify | 3e37d8d | **Evidence:** 04:30:30Z ask "timed out" at the 4s bell with the reply landing right at it (strike 1); 04:30:42Z the DELIVERY ask - actively streaming spoken partials - hit its absolute deadline (strike 2) and the two-strike watchdog killed pid 96820 mid-sentence (exit -1073741510, last_command = the delivery prompt); red banner. Addendum 04:46:15-25Z: same policy killed the respawn mid-HEARTBEAT (non-streaming ask, record-less progress invisible to the old policy); operator sat through a 135s Lex turn in silence. 04:31:52Z: respawn warmup killed at the 45s bound (healthy boot measured 27s; under load it blew through). **Root cause:** absolute per-ask deadlines are the wrong liveness shape - they bound total duration when what indicates death is SILENCE, and a timed-out ask counted a strike even when the session was provably generating. **Fix:** (1) streaming asks: caller deadline now bounds time-to-FIRST-record only; each new record extends the effective deadline by an idle grace (`DEVNEURAL_VOICE_BRAIN_STREAM_IDLE_MS`, 15s) capped by an absolute wall (`DEVNEURAL_VOICE_BRAIN_STREAM_MAX_MS`, 120s) - an actively-streaming delivery can no longer time out, so the watchdog can never kill mid-speech. (2) Strike policy isolated in `_shouldCountLivenessStrikeImpl`: a timeout strikes ONLY when the session showed zero life - no records AND no jsonl growth (covers heartbeat asks; jsonl growth = claude picked the prompt up). Structured as the single drop-in point for the operator-pending signal-based redesign (jsonl growth OR pty output at every phase; do not finalize until confirmed). (3) Defaults matched to measured latency: turn ask 4s → 8s, render/delivery first-record 3s → 8s, warmup 45s → 90s. (4) `voiceLexReply` returns 'delivered' \| 'cut' \| 'miss'; a 'cut' delivery (partials spoken, stream died) is RE-SPOKEN in full once the brain is warm again (`isVoiceBrainSessionWarm` poll, 90s, one retry, raw-body fallback), never dropped - with a `deliverySeq` guard so a newer reply supersedes a stale redelivery. **Known interim hazard (documented):** a slow session surviving a timeout can emit its reply late; the serialized ask queue + baseline-at-inject bound the crosstalk window; the signal-based redesign subsumes this. **Tests:** idle extension resolves past base deadline; stalled-after-partials twice never kills; heartbeat-shaped record-less growth never strikes; zero-signal timeouts still kill on the second; absolute wall bounds a runaway stream; outcome union + redelivery pins. |

### Third wave (operator-directed, 05:00Z+)

| # | Title | Status | Commits | Evidence + fix |
|---|-------|--------|---------|-----------------|
| SM-8 | Voice status pill stale/inverted "muted (tts)" label masks "speaking"; two divergent label maps | ✅ shipped, awaiting live verify | (this commit) | **Evidence:** operator-reported. The TopBar pill's overlay ternary keyed on `micGated`, which is true for the ENTIRE TTS playback window, and rendered "muted (tts)": semantically inverted (the gate pauses the MIC while TTS plays; TTS is not muted) and it replaced "speaking" for the whole audible reply - the pill read "muted (tts)" while Lex was talking out loud. Operator rule confirmed live: a "muted" label may only appear on an explicit user mute. Second defect: the full panel header carried its own divergent label map ("Lex thinking…" vs the pill's "thinking"), so the two surfaces could drift. **Fix:** new `08-dashboard/lib/voice-status.ts` - canonical `VoiceStatus` union, `VOICE_STATUS_LABEL` map, and `resolveVoiceStatusLabel(status, {softMuted, micGated})`. Precedence: (1) softMuted -> "muted (voice)" (only an explicit user mute shows a muted label); (2) status=speaking is NEVER masked by micGated; (3) a gate that outlives playback reads "mic paused" (what the gate actually does); (4) base label. Panel header and VoicePillView both resolve through the shared module (inline maps deleted); tones key off the resolved overlay. **Reachability audit (post-phase-2 pipeline, streaming partials + semantic endpointing):** all 8 statuses confirmed live - idle/connecting (lifecycle + reconnect), ready (hello-ack), listening (VAD onSpeechStart), transcribing (utterance-end paths incl. cap-fire + mute-flush), thinking (transcript/injected/finalize-injected), speaking (tts-start), error (WS/mic/audio failures); server overlay frames confirmed emitted (voice-mute/voice-unmute lex-voice-ws.ts:2924/2928; tts-start/tts-end protocol). No states removed; softMuted+speaking cannot co-occur (tts-start early-breaks under soft mute). **Tests:** 8 resolver pins (`tests/voice-status.test.ts`: exhaustive base map, speaking-never-masked, mic-paused, soft-mute precedence, retired-label ban) + 3 pill integration pins; dashboard suite 215 passed (was 204), tsc clean. |

| SM-9 | Smart-clear investigator draft content-empty on worker anchors (vet rejected, Lex hand-authored the reseed) | ✅ shipped, awaiting live verify | (this commit) | **Evidence:** POST /lex/smart-clear/plan 05:11:01Z logged `has_content=false vet_ok=false`; the audit row's payload_text confirms the draft carried ONLY git signals (HEAD 8f1c111, dirty flag) - no active work, no next step - so vetReseed rightly rejected it and Lex authored the 05:14Z reseed by hand. **Root cause (verified against the live db):** the plan route targets a WORKER anchor (project_session id 6c7d6691..., title "DevNeural-433a2f"); `assembleInvestigatorContext` is brainstorm-anchored and FAILS CLOSED when `getBrainstorm(anchorId)` has no row (lex-investigator.ts:171) - confirmed live: brainstorm row null, project_session row present. The sweep returned empty before ever reading docs or transcripts, leaving `extractThreadHints` nothing, so `draftReseed` emitted the git-only skeleton. **Fix (additive, vet gate untouched):** worker-anchored activity source in smart-clear.ts. The route resolves the worker's live jsonl via the existing `jsonlForAnchor` and passes `workerJsonlPath` into `assembleSmartClearReport`; new pure `extractWorkerActivity` tail-reads the jsonl (bounded 256KB fd read in prod) and extracts (a) the LATEST substantial user directive (>=80 chars, system-reminder-stripped; tool_result-only and short steering turns skipped) = the worker's current task + queue + constraints verbatim, plus the latest assistant text; `buildWorkerActivityBlock` leads the report with it; structured `workerHints` feed doing/next/decisions directly into `draftReseed` (heuristic line-scan only fills gaps). Bounded slices keep the draft inside the vet's 2400-char/40-line caps. Empty tail degrades to exactly the old behavior - no fabricated content for the gate to bless. **Tests:** 9 new pins in tests/smart-clear-report.test.ts incl. a regression pin of the 05:11Z failure shape (no jsonl -> hasContent=false + vet "no next step"), the full worker path passing the UNCHANGED vetReseed, huge-directive boundedness, and both-sources merge. Daemon suite 1842 passed (was 1832), tsc clean. |

| SM-10 | Smart-compact session-ready wait too short (15s budget vs ~20s observed /clear->new-jsonl gap) | ✅ shipped, awaiting live verify | (this commit) | **Evidence:** 05:14:05Z `smart-compact: session-ready-wait-timeout reason=timeout-new-jsonl elapsed=15078ms`; the clear+resume still succeeded but only via the fallback ship (`resume ship_ok=true`), and the new jsonl appeared ~20s after fire. The gate was tuned to a ~7s spawn; under tonight's load the spawn blew through the whole 15s budget before the jsonl even existed. **Fix:** `awaitNewSessionReady` default budget 15s -> 45s (`DEFAULT_SESSION_READY_TIMEOUT_MS`), covering the observed window with margin; per-host override via `DEVNEURAL_SMART_COMPACT_READY_TIMEOUT_MS` (>=1000ms to take effect). Both callers (fire + clear-and-paste routes) ride the default. The fallback back-to-back ship path is untouched and stays the safety net past even the widened budget. **Tests:** 2 new pins - default budget rides out a virtual-clock 20s spawn and settles on the SessionStart chain; env override shortens the budget and is cleaned up. Suite 1845 passed, tsc clean. |

| SM-11 | Front-truncation of voice payloads pasted into the Lex terminal (three losses tonight; delivery confirmation was submission-only) | ✅ shipped, awaiting live verify | (this commit) | **Evidence:** three live occurrences, all voice->Lex paste path, all losing LEADING payload content while the daemon-side transcript stayed intact: ~04:41Z utterance arrived mid-word ("sing a timer..."); 04:54Z a 55-word utterance landed as only the tail fragment "...ood that?"; ~05:20Z the prompt held "context never ran.[voice mode]" with the whole lead eaten (screenshot 76c867fa) - that remnant matches the FLUSH payload shape (voiceTag rides the END on the queue-flush path). Distinct from the 8f1c111 swallowed-Enter bug: these turns DID submit, truncated. The 8f1c111 verifier could not catch them - its fingerprint was the utterance head only, and it verified submission, not integrity; a CR retry cannot restore lost content anyway. **Fix:** content-integrity extension to `_verifyInjectDeliveryImpl`. New `payloadIntegrityFingerprints(payload)` derives single-line head + tail probes (first/last non-empty line, bounded, never crossing a newline so composer normalization cannot mask a hit). Both call sites (direct turn inject + mid-turn queue flush; `_flushPendingUtterancesImpl` now returns its injected payload) pass the FULL payload through; the verifier requires every provided probe in ONE user record to confirm INTACT. A record carrying some-but-not-all probes classifies as a PARTIAL landing and fires ONE full repaste of the payload (`INJECT PARTIAL DELIVERY` logged loudly), then keeps scanning for the intact record; persistent partials exhaust into the existing loud failure + client error frame. No-probe callers and the no-record stuck-paste path behave exactly as in 8f1c111 (CR retries, space+Enter escalation, failure frame). **Tests:** 7 new pins (probe derivation incl. blank/single-line payloads; intact = zero repastes; the ~05:20Z front-truncation shape repastes once then confirms; the 04:54Z tail-only shape repastes; repaste bounded at one with persistent partials failing loud; stuck-paste CR path unchanged) on top of the 7 existing 8f1c111 pins. Suite 1851 passed, tsc clean. |

| SM-12 | Replay-on-switch re-spoke stale, already-heard replies on every voice reconnect (four firings tonight, up to 93s stale) | ✅ shipped, awaiting live verify | (this commit) | **Evidence:** daemon.log replay-on-switch firings at 04:33 (age=2s), 04:39 (age=76s), 04:48 (age=43s), 04:59 (age=93s). The operator stop/started voice repeatedly because of the silence bugs and every reconnect re-spoke the previous reply, including fully-heard ones ("why do you keep saying that"). **Root cause:** the replay gate was age-only with an 8-MINUTE window and no notion of whether the reply had been delivered; the once-per-bind guard is per-connection closure state, so every reconnect re-armed it. **Fix:** stale-replay gate in lex-voice-ws.ts (`_shouldReplayOnBindImpl`, pure): the audio replay now requires BOTH (a) reply age <= ~10s (`REPLAY_MAX_AGE_MS`, env `DEVNEURAL_VOICE_REPLAY_MAX_AGE_MS`; the 8-min `REPLAY_WINDOW_MS` remains solely the digest-seeding recency) AND (b) the reply was NOT fully delivered - the 3e37d8d delivered/cut/miss outcome is the signal: only cut or miss (or no record at all, i.e. the reply landed while no client was attached) may replay; delivered never does. Outcomes are stamped per session in a MODULE-level map (`_recordReplyDelivery`, keyed by jsonl) so they survive the per-connection closure across reconnects; `speakViaBrain` records the final outcome on every path (delivered, cut->redelivery upgrade, miss, raw-fallback counts as delivered since it enqueues the full body). A record predating the turn is ignored (it covered the previous reply). Skips log the reason (`replay-on-switch: SKIPPED (...)`). All four of tonight's firings die under the new gate: 76/43/93s to the age gate, the 2s one to already-delivered. **Tests:** 10 new pins (cut/miss replay, delivered never replays, the 04:33Z and 04:39Z shapes, no-record-while-away replays, stale-record-ignored, ~10s default, module-level tracking + reset). Suite 1862 passed, tsc clean. |

| SM-13 | Talk layer fabricated first-person speech in Lex's voice (invented memory claims, answered complaints itself, promised actions it cannot take) | ✅ shipped, awaiting live verify | (this commit) | **Evidence:** operator repeatedly heard "I can't see the last thing I said - if it's in a chat I can't access it because you have a fresh start" - a line Lex (brainstorm 04689e21) never wrote. Session ed330939: the talk layer received the operator's complaint about that exact line and replied IN LEX'S VOICE ("Understood, and you're right to call it out... I'll flag it..."), promising supervisor actions, plus a FORWARD. Session 074b63b4 shows the mechanism: the persona prompt fed RAW digest fragments (Last decision containing "fresh start", Open question "Say it again?", EMPTY "Last line you spoke") and the model recombined them into the invented line. **Fix (deterministic guards + prompt hardening, kill-canned-glue mandate):** (1) voice-top-layer.ts pre-ask bypass - complaints / prior-turn meta (`referencesPriorTurns`) and repeat requests with NOTHING to repeat (`isRepeatRequest` + empty lastSpoken) forward silently WITHOUT the talk model ever seeing the turn; repeat with a real last-spoken line still answers verbatim. (2) `guardTopLayerSpeech` classifies fabrication speech: first-person memory/visibility claims (SELF_CLAIM_RES: "I can't see/access/recall...", "my/your memory/context/chat/history", "fresh start") and action promises (ACTION_PROMISE_RE: "I'll flag/report/escalate/..." - bare "I'll check" stays legal, it is the sanctioned handoff). Guarded speech is held mid-stream (never emitted via onSpeech) and stripped from the final parse; when the strip leaves no forward/control the utterance forwards, so the operator's words always reach Lex. (3) topLayerSystem contract now states all three rules (complaints -> FORWARD only; never promise actions; never talk about own memory/visibility). (4) voice-persona.ts contextualizes the digest: `DIGEST_CONTEXT_NOTE` marks the lines as third-party notes about the WORK ("NOT things you said... never quote a fragment as a claim about yourself"), and empty fields render as `(none)` instead of bare blanks. **Tests:** 12 new pins - complaint never reaches the model (silent forward), repeat-with-nothing forwards instead of inventing an excuse, repeat-with-lastSpoken still answers, the live invented line class strips + forwards, the ed330939 promise shape strips speech but keeps the model's FORWARD, digest-grounded benign speech untouched, streamed fabrication never reaches onSpeech, streamed promise held+stripped, guard/matcher unit pins, system-contract pins, persona (none)+contextualization pins. Suite 1876 passed, tsc clean. |

| SM-27 | Distillation not guaranteed on EVERY brainstorm end: the kill/reaper path ended a brainstorm with zero distillation and defeated the onExit distiller; grooming day-cap flipped ended before distilling | ✅ shipped, awaiting daemon restart + live verify | (this commit) | **Operator directive (2026-07-18):** every brainstorm session end - End button, PTY close, crash, anything - must leave the last session distilled and prepared for cold-start preload, no exceptions. **Gaps found auditing every status-ending mutation (brainstorm_sessions only; project_session/worker paths are a separate preload and explicitly out of scope):** (1) `ptyHost.ptyKill` (taskkill /F /T, switch-session, daemon-restart PTY teardown, or onExit-never-fired) called `endBrainstorm` DIRECTLY with no distillation, AND because it flips status='ended' synchronously it defeated the async onExit handler's own distiller (guarded on status==='active'), so even a normally-covered exit lost its distillation when it went through a kill. (2) `grooming.finalizeDayCap` flipped lifecycle_state/status='ended' BEFORE calling runFinalDistillation, so a daemon death in that window left an ended brainstorm with no pending-distill marker and no recovery. **Fix:** ptyKill now routes cc-pty brainstorm ends through the same SM-23 pending-distill queue (marker persisted, session-end lock + in-flight join dedupe against End button / onExit so no double run; endBrainstorm deferred to the run's finally); direct-llm keeps the direct end (no PTY = a kill is not a Lex stop). Grooming day-cap now awaits distillation FIRST (queueSessionEndPipeline writes the marker synchronously and the terminal pipeline does the ended flip), then the explicit flip is belt for the prod-unreachable unwired case, which now logs DAY-CAP WITHOUT DISTILLATION loudly instead of silently ending stale. Combined with SM-23's crash-recovery marker + cold-start forced-distill gate, every brainstorm end now writes the marker before/around the status flip, so a fresh session on the anchor always cold-starts on real distilled context. **Tests:** 2 new grooming pins (distill-before-ended-flip; unwired logs loudly + still ends); distill-pending 8 + session-end 8 + idle-watcher + pty-host-logger green (22). tsc clean. |

| SM-26 | Cold-start greeting presented day-old distillation as current + opened with OTLC-Brainstorm.md boot noise | ✅ shipped, awaiting daemon restart + live verify | d79f1e9 | Operator caught a fresh brainstorm's first reply reciting previous-day state as if current and opening with "No OTLC-Brainstorm.md in project". Preamble distilled stamp was bare HH:MM (no date) so a 20h-old distillation read as earlier-today; past 6h the line now carries explicit age + "trust the appended recent turns" hint. System prompt now tells brainstorm anchors the OTLC-Brainstorm.md session-start rule is worker-level and to skip it silently, opening with the anchor's grounded state. **Deeper issue logged:** the brainstorm-chunks fallback writes a fresh-stamped last_summary with recycled content on no_session_scoped_chunks, defeating timestamp-based freshness verdicts (needs content-aware verdict). |

| SM-25 | Concurrent utterances produced stacked discrete voice replies instead of one cohesive answer | ✅ shipped, awaiting daemon restart + live verify (coalesce) / LIVE-VERIFIED (transcript) | bda4ad8 | Operator: if he barges / adds a second thing before the top layer replies, the brain should combine into one cohesive reply, not stack. Top-layer turns now serialize (topTurnInFlight + pendingTopUtterances); an unspoken in-flight reply is superseded and re-asked with everything combined (mergeOperatorUtterances), a streamed reply finishes then drains the queue as one follow-up. Depth-capped 6; controls never superseded. 5 pins. Verified live in the operator's transcript (one merged reply). |

| SM-24 | Voice capture dead on the wave client: stuck-open recovery raced redemption and discarded every utterance | ✅ shipped + LIVE-VERIFIED 15:14Z | (this commit) | **Evidence:** operator voice dead all afternoon on the 07-14..07-18 wave client while the pre-wave bundle worked (04:55Z SW-cached session; live A/B ~14:50Z: old client + current daemon = Lex replied). Capture probe (voice_health "cp:" rows) on the new client: capture ctx running, worklet ~125fps, silero fed, m=1.00, la=0.95 speaking / 0.01-0.04 silence, ss fires, se=0 ALWAYS - speech opened, never closed, audio never shipped, zero `transcript received` in daemon.log. **Root cause (arithmetic):** the wave's stuck-open recovery in onFrameProcessed evaluated a fixed 1500ms rolling prob window against the 0.4 floor; the average crosses below 0.4 after ~915ms of normal end-of-utterance silence, while FrameProcessor redemption needs 1400ms (14 legacy frames = 1344ms). Recovery always fired first; vad.pause() with vad-web default submitUserSpeechOnPause:false runs reset() which DISCARDS the buffered utterance and never emits SpeechEnd. Pre-wave client had no such recovery. **Fix:** recovery window now derives from the live redemption setting (max(1500, redemptionMs + 1500)) so legitimate silence always closes via redemption first; recovery still recycles a genuinely stuck listener. Also this session: MicVAD reverted to its own getUserMedia grant (synthetic GainNode feed retired; probe showed clean probs on own-grant), server prefs healed (mic_gain 0.4->1.0, vad_sensitivity 0.1->0.5, vad_redemption_ms 2500->1400; the daemon re-pushes prefs per connect so no client reset could ever heal them), ORT wasmPaths cache-busted (?v=st1; PWA hard reset does not clear the HTTP disk cache, a truncated 13MB wasm entry caused the "no available backend found ... RangeError" cascade), vad-err + capture-probe telemetry mirrored to voice_health. **Live verify:** 15:14:32Z `transcript received words=8 text="1, 2, 3, 4, 5, 6, 7, 8."` on the current client. **New open item from the same test:** status stuck on "speaking" - tts-start arrived, zero PCM frames for 30s+ (frame_timeout heal_failed 15:16Z), no `top-layer turn` line after the transcript; server-side reply/synth stall under heavy box load. Daemon-side; needs its own investigation. |

| SM-23 | End button on brainstorm session hangs; end-session hook + distillation pipeline unverified; restart-before-distillation leaves the new session cold-starting on stale context | ✅ shipped, awaiting daemon restart + live verify | (this commit) | **Operator report (2026-07-18):** pressing the End button on a brainstorm session hangs the UI. Scope of the fix, all four parts required: (1) **End must not hang** - find and fix the hang itself, and the end path needs a hang guard (bounded waits, no unresolved awaits blocking the close). (2) **Verify the ENTIRE end-session hook chain + distillation actually runs end to end** (session-end pipeline -> chunks -> distill -> drafts/thread-doc), not just that the button returns. (3) **Restart-before-distillation ordering:** if a brainstorm session is started while the PREVIOUS brainstorm's distillation has not yet completed, FORCE that distillation to run, and the new session must WAIT for it to finish before its cold-start hooks (cold-start-preload) run - otherwise cold start reads stale/missing distilled context. (4) **Operator-visible progress:** the UI must indicate what is happening during the wait (e.g. "finishing previous session: distilling..."), not sit silent. **Constraints from operator:** be careful with race conditions and with introducing new hangs (the wait must be bounded + observable); added latency is acceptable ONLY on a true complete cold start, and expected there. |

| SM-9b | SM-9 live-probe follow-up: queue-operation directives invisible, 256KB tail too small, worker-block header leaked into hints | ✅ shipped + live-verified | (this commit) | **Evidence (live probe of the freshly restarted daemon, 06:2xZ):** POST /lex/smart-clear/plan on the worker anchor returned has_content=true (SM-9 works) but vet_ok=false with the draft carrying `Were doing: Worker active context (live session tail)` - the worker block HEADER, grabbed by extractThreadHints' first-line fallback on the combined report. Root causes found by probing the real session jsonl: (a) mid-turn operator messages do NOT land as type:"user" records - the harness stores them as `{type:"queue-operation", operation:"enqueue", content:"<full text>"}` (every QUEUE ADDITION tonight is one), so the user-record scan missed every live directive; (b) the newest directive sat >2MB behind EOF in the 2.1MB tool-heavy session - the 256KB tail window saw none of it. **Fix:** extractWorkerActivity also accepts queue-operation records with string content (same >=80-char floor + bounds); WORKER_TAIL_BYTES 256KB -> 4MB (bounded fd read, still cheap); heuristic hints now scan the INVESTIGATOR block only, never the combined report, so worker-block scaffolding can never become a hint. **Tests:** 2 new pins (queue-operation directive extraction; reply-only tail never leaks the header into the reseed). **Live verification after rebuild+restart:** plan route returns vet_ok=true with the draft carrying the real current task, queue, and constraints. Suite 1878 passed, tsc clean. |

### Dashboard goal sweep (2026-07-16 operator audit)

| # | Title | Status | Commits | Evidence + fix |
|---|-------|--------|---------|-----------------|
| GS-1 | Supervision default is now EVENT everywhere; selector renders on every anchored project and survives small screens | ✅ shipped, awaiting rebuild | (this commit) | **Operator report:** default should be event (found one on polling, fixed by hand); selector invisible on small form factors; most project cards showed no selector at all. **Root causes:** (a) hard-coded 'polling' fallbacks in `getDefaultSupervisionMode` and `toAnchorView` (runtime_config already said event, but a missing/garbage row fell back to polling); (b) 31 of 35 anchors carried an EXPLICIT 'polling' stamped by the seed backfill - explicit beats default, so the event default never applied; (c) `/projects/anchor-tiles` filtered `status:'live'` only, so dormant anchors (31/33) got no tile and ProjectsGrid's `anchor &&` gate hid the selector; (d) the selector row used no wrap and the tile's overflow-hidden clipped the pill at narrow widths. **Fix:** fallbacks flipped to 'event' (code + comments + toggle tooltips: event="default", polling="opt-in fallback"); migration 051 backfills seeded polling->event ('off' untouched - the kill-switch is an operator choice); tiles route gains `?status=all` (default stays live-only for the Stream Deck contract) and ProjectsGrid requests all anchors; selector row is now flex-wrap so the toggle wraps onto its own line instead of clipping. **Wiring verified:** worker-event-listener gates on per-anchor mode ?? db default (worker-event-listener.ts:364); PATCH /projects/:id persists. Same ProjectsGrid component serves home + projects pages, so both get every fix. **Tests:** default-supervision-mode pins flipped to the event contract; new dormant-inclusion pin in projects-anchor-tiles; supervision-mode-patch default pin updated. Daemon 1879 passed + tsc clean; dashboard 215 passed + tsc clean. |

| GS-2 | Settings cards: "Auto-reset" renamed to Worker auto-clear; history table gets headers + plain-English actions; scary runtime/env dump replaced with one effective-mode line (smart-compact + auto-advance) | ✅ shipped, awaiting rebuild | (this commit) | **Operator report:** header "Auto reset stuck workers" reads as a different feature than the auto-clear it is; the history table (noop / wrap / clear-and-paste rows) had no column headers and raw tokens; "runtime: live env: DEVNEURAL_SMART_COMPACT_ENABLED=(unset -> shadow)" looked broken ("WTF, we tested this yesterday"). **Truth:** it was never broken - runtime_config.smart_compact_mode=live overrides the env fallback; the footer just printed both layers raw. **Fix:** SmartCompactPanel header -> "Worker auto-clear" with an honest subtitle ("(Internally: smart-compact)"); footer -> one line: "Effective mode: live - set from this dashboard toggle." (env/default variants when runtime unset), same treatment on AutoAdvanceModePanel. SmartCompactAuditPanel gains a sticky column-header row (when / what happened / why / context / worker / by) and plain-English action labels with the raw token as tooltip: fire + clear-and-paste -> "cleared + resumed", wrap + wrap-paste -> "asked to wrap up", shadow -> "logged only", noop -> "no action"; the client action union previously missed the two Lex-driven v2 actions entirely (they rendered unstyled). **Scope check (operator asked "does this work on all brainstorm/worker pairs"):** yes - the trigger loop enumerates ALL live anchors (auto-advance-supervisor.ts:339, limit 200) and the mode is global. **Auto-advance audit (task 6):** header/blurbs already plain; runtime live; supervisor gates on event-mode anchors; no defects found beyond the shared footer dump. **Tests:** 4 new pins (header rename, effective-mode line, column headers, friendly labels incl. v2 actions). Dashboard 219 passed, tsc clean. |

| GS-3 | Wiki match history: column headers, plain English, click-to-expand verdict with the injected preview | ✅ shipped, awaiting rebuild | (this commit) | **Operator report:** "cos"/"raw" jargon meaningless, no headers, rows dead - can't see accepted/rejected or what was injected; "how do we know if this worked, I don't remember seeing anything injected". **Verified working:** the pipeline IS live - reinforcement.log.jsonl shows injections firing today (15:19Z, 15:26Z) with outcomes (raw-no-hit similarity 0.45), and the `<devneural-context>` blocks that ride session prompts ARE those injections landing; they were just invisible on the dashboard. **Jargon decoded:** "cos 0.45" = cosine similarity between the prompt and the matched content; "src raw" = the match came from a raw transcript chunk (vs a wiki page). **Fix:** daemon injection events now carry a bounded 280-char `preview` of the injected summary/chunk (recordInjection + recordRawInjection); ReinforcementPanel gains a sticky header row (when / what happened / matched page-chunk / details), plain-English kind labels ("sent to worker", "match used", "match unused", "corrected", "queued for wiki"; raw token in tooltip), details say "similarity 0.82 · transcript chunk" instead of "cos 0.82 · src raw", and every row expands to a one-sentence verdict (sent / accepted / not used / rejected + what happened to the weight) plus the injected preview and session id. Old log lines without preview degrade gracefully. **Tests:** 2 daemon pins (preview present, bounded) + 4 panel pins (headers, no jargon, expanded preview, accepted/not-used verdicts). Daemon reinforcement tests 6 passed; dashboard 223 passed; tsc clean both. |

| GS-4 | "Dirty" raw chunks on /system: bounded by a periodic flush + honest badge copy | ✅ shipped, awaiting rebuild | (this commit) | **Operator report:** "why do I have dirty raw chunks showing in settings page, why does brain diagnostic show dirty and why isn't that fixed". **Truth:** dirty is not a fault - it means in-memory vectors written since the last .vec disk snapshot (the append-only meta sidecar already provides between-flush durability). But the unsaved window was UNBOUNDED: flush only ran on shutdown, the backup /flush handshake, and signal-coalescer passes, so during active capture raw_chunks sat "dirty" indefinitely (live check: dirty=true at 15932 chunks). **Fix:** (a) new `src/store/periodic-flush.ts` - dirty-gated tick, flushes at most once per interval (default 5 min, `DEFAULT_VECTOR_FLUSH_INTERVAL_MS`), never throws, unref'd timer; wired at daemon startup over Store.flush(). (b) DiagnosticsPanel badge: warning-toned "dirty" -> neutral "saving soon" with a tooltip explaining it is normal during capture and auto-saves within 5 minutes. **Tests:** 6 pins (flush-when-dirty, skip-when-clean, failing flush and throwing isDirty both resolve 'failed' without throwing, interval ticks + clean stop, 5-min default). Daemon 1887 passed, dashboard 223 passed, tsc clean both. |

| GS-5 | Reminders bell: no more pinned 9+ (signal chatter off the bell), clear-all sweep | ✅ shipped, awaiting rebuild | (this commit) | **Operator report:** "the reminders bell always shows 9; it should only show reminders set and active by me, or things I need to take action on from Lex; and it needs a clear all". **Root cause:** the bell surface admitted every 'signal'-class notification (reinforcement hits, supervision noise, push events), so unread stayed >= 9 forever and the badge clamps to "9+". **Fix:** bell filter now keeps reports + followups (reminders / Lex needs-you items) always, and 'signal' rows only at ALERT severity (real emergencies); info/warn signals stay on the right-rail activity feed, untouched. New `dismissAllNotifications(scope)` + POST /notifications/dismiss-all (scope required so a stray call can never blank both surfaces) + a "clear all" button in the bell dropdown with optimistic cache invalidation. **Tests:** filter pins updated to the new contract + 2 new pins (alert-only signal passage with activity rail intact; scope-isolated clear-all). Daemon 1889 passed, dashboard 223 passed, tsc clean both. |

| GS-6 | Daily brief: refresh regenerates for real; digest self-heals past 24h | ✅ shipped, awaiting rebuild | (this commit) | **Operator report:** "dashboard brief is still stale, and refresh button does nothing". **Root cause:** the brief renders wiki/whats-new.md, which only regenerated on the WEEKLY lint cycle; the refresh button merely refetched the GET, re-serving the same stale file. **Fix:** (a) refresh button now POSTs /whats-new (regenerates the digest daemon-side - pure file aggregation, no LLM) then refetches; spinner + "rebuilding…" while pending. (b) Self-healing: GET /dashboard/daily-brief regenerates inline whenever the digest is missing or older than 24h (`shouldRegenerateWhatsNew`, `WHATS_NEW_REGEN_THRESHOLD_HOURS`), best-effort with the stale copy as fallback. **Tests:** 4 daemon pins (missing-file, threshold boundary, custom threshold, 24h default) + 1 panel pin (refresh click fires the regeneration call). Daemon 1893 passed, dashboard 224 passed, tsc clean both. |

| GS-7 | Stream deck: worker sessions nest under their brainstorm | ✅ shipped, awaiting rebuild | (this commit) | **Operator ask:** "slightly nest a worker session under its brainstorm session". **Fix:** lex anchor tiles now carry `supervised_project_slug` (lex_session.supervises_project_anchor_id resolved to the worker anchor's project_slug via an injected resolver; pure helper `supervisedSlugFor`, never throws). StreamDeck renders the supervised worker's session tiles indented (ml-4 + left border) directly under the brainstorm tile and drops that group from the flat list below, so the brainstorm/worker pair reads as one unit; unbound brainstorms and unsupervised projects render exactly as before. **Tests:** 3 pins on the resolver (resolves, null on unbound/missing/unknown, throw-safe). Daemon 1895 passed, dashboard 224 passed, tsc clean both. |

| GS-8 | Curator feed: clear-all + destination hints; terminal mirror labeled with what it watches | ✅ shipped, awaiting rebuild | (this commit) | **Operator report:** curator feed needs a clear button; clicking a feed item dropped him into "a terminal mirror - what is that, what's it for, does it work, how do I know what project I'm watching". **What the mirror is:** a live READ-ONLY view of a session's terminal (xterm over the daemon's terminal-stream WS); feed items whose link targets /lex or a session detail land there. It works - it just never said what it was. **Fix:** (a) curator feed header gains "clear all" (dismissAllNotifications scope 'activity'; the bell keeps its own rows); (b) every clickable feed row now shows its destination ("→ opens wiki page" / "→ opens live terminal") via `feedDestinationLabel`; (c) TerminalMirror header gains a watch label - "watching <project> · session <id8> · read-only" (`mirrorWatchLabel`, honest fallbacks for unknown project/unbound session) with a tooltip stating keys typed there are never sent. **Tests:** 4 pins on the two helpers incl. fallbacks. Dashboard 228 passed, tsc clean. |

| GS-9 | Wiki problems: 6,972 duplicate lint findings root-caused + collapsed; drafts empty-state explains itself | ✅ shipped, awaiting rebuild | (this commit) | **Operator report:** "I have wiki problems that are unresolved, make that all make sense" + "no new wiki drafts, does that make sense". **Findings root cause:** 6,972 of 6,993 open audit findings were ONE lint finding ("archive pending stale") duplicated DAILY since June 3 across 166 pending pages - the content-derived finding id hashed the raw detail text, whose day counter ("pending 73d, no hits") increments every run, minting a fresh id per day per page. INSERT OR IGNORE never had a chance. **Fix:** `stableLintFindingId` normalizes digits out of the hashed detail (same page+kind+shape = one row forever; distinct shapes still distinct); migration 052 backfill-resolves all but the newest open lint finding per (page_slug, finding) - post-migration the open count drops from ~6,993 to ~187 honest items (166 stale-pending pages + 21 janitor contradiction flags). **Drafts:** the pipeline is healthy - all 6 drafts ever produced were reviewed and promoted; "no pending drafts" was TRUE but read as broken. Empty state now explains when drafts appear (brainstorm session end, 200+ chars, local-model distillation) and that empty = caught up. **Tests:** 2 pins on id stability across the day counter + distinctness across pages/shapes. Daemon 1898 passed, tsc clean both. |

| GS-10 | Sessions page: one merged table (start buttons + status + id + state, no separate section) | ✅ shipped, awaiting rebuild | (this commit) | **Operator directive:** "combine this table, I don't want separate status below the start claude section... this can all be one table with the status and start buttons and indicators and ID and all that". **Fix:** SessionsTable renders ONE table. Ready-to-start projects become rows (Session ID "—", status "not running" with idle dot, Action = Start Claude button with the existing pending/"starting…" state); live/idle/stale session rows keep project link, id, status dot, captured-state chips, last activity, and gain an "open" action. The "Ready to start (N)" section is gone; header counts and the idle/stale reveal toggles are unchanged. **Tests:** 2 pins (single table containing both row kinds with the start button inside it and no section header; startable rows read "not running"). Dashboard 230 passed, tsc clean. |

| GS-11 | Home KPI tiles wired for real: artifacts/brainstorm + wiki lineage computed from wiki_drafts | ✅ shipped, awaiting rebuild | (this commit) | **Operator report:** "why does artifacts / brainstorms under the dashboard home say not wired yet and pending, can we finish that". **Root cause:** /stats/brainstorm-kpi hardcoded `artifacts_per_brainstorm_avg: 0` and `wiki_lineage_coverage: 0`; the tiles rendered any 0 as a permanent "pending / not wired yet" badge. **Fix:** new `computeBrainstormKpis(db)` (src/dashboard/brainstorm-kpi.ts) computes both from wiki_drafts - artifacts/brainstorm = drafts per brainstorm session; lineage = fraction of brainstorms with at least one draft (live data: 6 drafts over 16 brainstorms -> 0.4 avg, 13% lineage). Route delegates to it; tiles drop the pending flags (a zero is an honest zero) and subs state the real definitions. The curator card's "canary: not wired" pill is left as-is - it honestly labels a daemon probe that does not exist yet (real feature work, out of scope tonight). **Tests:** 2 pins (computed ratios on a seeded tmp db; zero-safe empty db). Daemon 1900 passed, dashboard 230 passed, tsc clean both. |

| GS-12 | Home tiles show status instead of duplicating the supervision control; meeting tab audited (one dead link fixed) | ✅ shipped, awaiting rebuild | (this commit) | **Task 9 (operator: "should these projects and selectors even be duplicated on home pages... perhaps that shows me something else, seems redundant"):** agreed - controls should live once. Home (compact) project tiles now render a READ-ONLY status row instead of the toggle: worker phase with a tone-matched dot (thinking/tool = ai, permission = warn, else live/idle) plus "supervision: <mode>" text pointing at /projects for changes. The full /projects grid keeps the interactive toggle (with the GS-1 wrap + dormant fixes). If you want different content on home tiles, say the word. **Task 11 (meeting tab, untested):** audited end to end - /meetings + /meetings/:id routes are coherent (consent gate, audio purge countdown, action items, PATCH family, no auto-distillation per BF-15); the UI list filters/rows/consent chips render sanely. ONE break found and fixed: the empty state linked to /sessions/new, a route that does not exist (dead 404), and never named the real start path. It now points at the voice panel's meeting toggle in notes mode and explains the consent-gated audio retention. **Tests:** 2 grid pins (compact = status row, no toggle; full = toggle, no status row) + 1 meeting empty-state pin. Dashboard 233 passed, tsc clean. |

### Fourth wave (2026-07-17 00:49-00:56Z live voice failures)

| # | Title | Status | Commits | Evidence + fix |
|---|-------|--------|---------|-----------------|
| SM-14 | Voice ws dropped every 30-60s; no close reason ever logged | ✅ shipped, awaiting rebuild | 7d4222a | **Evidence:** ws-close 00:50:27 / 00:51:05 / 00:54:58 / 00:56:47; operator toggled voice all evening. **Root cause:** an idle voice session sends ZERO frames and nothing drove the protocol's ping/pong, so an idle-timeout reaped the socket. **Fix:** client keepalive ping every 15s while open (server already pongs), cleared on close; server logs code+reason+bindKey+tts-state loudly on every ws-close (with a 1000/1001/1006 legend) and ws errors log before teardown. |
| SM-15 | Forward-inject false partial-delivery: repaste duplicated the turn, then FAILED logged on a delivered prompt | ✅ shipped, awaiting rebuild | 73508bd | **Evidence:** 00:50:09 INJECT PARTIAL DELIVERY -> repaste -> 00:50:22 INJECT DELIVERY FAILED; jsonl shows BOTH pastes landed (00:50:06 and 00:50:19, each genuinely front-truncated but with the full "[voice mode] ..." utterance intact) and Lex answered twice. **Fix:** the UTTERANCE fingerprint is the delivery signal - a word-carrying record confirms immediately; head/tail loss logs a loud TRUNCATED DELIVERY line, never a repaste; repaste + FAILED stay reserved for word-LESS fragments. FAILED is unreachable for delivered words. |
| SM-16 | Zero TTS all evening with zero log evidence | ✅ shipped, awaiting rebuild | 1cc3701 | **Evidence:** operator heard nothing; last_tts_ack_ms hours stale. **Trace:** replies landed in the 30-60s socket gaps and vanished in send()'s silent early-return; synth/pcm errors only pushed frames at the dead socket. **Fix:** speak() on a closed connection logs SPEAKABLE REPLY DROPPED (+text preview); first tts-* frame dropped after close logs one-shot at the send layer; controller logs TTS SYNTH FAILED / TTS STREAM ERROR to daemon.log via a wired log dep. With SM-14 (no gaps) + SM-17 (no chars=0 turns) the silence mode is closed or one-grep diagnosable. |
| SM-17 | Time-based liveness killed/nulled live sessions; signal-based redesign implemented | ✅ shipped, awaiting rebuild | 65a8dc0 | **Operator verdict:** "time based is likely bad, it is too short"; asks returned chars=0 at 6-7s while bytes_grew=true, producing silent turns. **Fix:** alive = jsonl growth OR pty output (pty-host lastActivity) within DEVNEURAL_VOICE_BRAIN_SIGNAL_QUIET_MS (15s), at EVERY phase: ask (streaming AND non-streaming) and warmup extend their effective deadline on each signal, bounded by the walls (stream 120s; warmup 3x base). DEFAULT_ASK_TIMEOUT_MS (6s) now bounds time-to-first-signal only. Kill fires only when all signals are quiet, and the warmup kill log says so. |

| SM-18 | ws-close ran the TERMINAL session-end pipeline: one socket drop ended the live brainstorm and locked voice out ('brainstorm ended') | ✅ shipped, awaiting rebuild | (this commit) | **Evidence:** operator hit 'brainstorm ended' on every voice bind at ~01:36Z; db showed brainstorm 4bbafb48 status=ended, ended_ms stamped by the session-end pipeline fired from a routine ws-close on the flaky pre-keepalive build. The bind gate correctly refuses terminal rows, so reconnects were locked out of the live session. **Live remediation (done, no restart):** reopened the row (status=live, ended_ms=NULL); operator rebinds immediately. **Root cause:** the trigger table ran runSessionEndPipeline (terminal, marks ended) for ws-close on cc-pty brainstorms; only direct-llm was flush-only. **Fix:** _sessionEndActionForReason - ws-close is NEVER terminal in any runtime mode (distillation flush only; brainstorm stays alive for the reconnect); terminal is reserved for explicit intent (spoken end-session, UI end, compaction-restart). Latch re-arms after every flush so an explicit end still fires. **Tests:** 1 pin (flush for ws-close; terminal for voice-command / compaction-restart / ui-end). Suite 1906 passed, tsc clean. |

### Fifth wave (2026-07-17 repeat-loop + distillation pipeline)

| # | Title | Status | Commits | Evidence + fix |
|---|-------|--------|---------|-----------------|
| SM-19 | Lex repeated the same reply in a loop during the 01:33Z boot (replay-on-switch re-fired on every reconnect) | ✅ shipped, awaiting rebuild | (this commit) | **Evidence:** daemon.log 01:33:13-01:33:18Z: ws flapped roughly every second while the daemon booted, and every fresh socket logged `replay-on-switch: speaking last reply (no delivery record (client was away))` at ages 5s/6s/7s/8s/9s - the SAME reply spoken five times until the 10s staleness cap silenced it. **Root cause:** the per-connection `replayedOnBind` flag dies with each socket, and the replay never recorded a delivery, so the "no delivery record" branch stayed true across reconnects. **Fix:** `_replayLastTurnOnBindImpl` - the replay IS a delivery; it stamps 'delivered' on the module-level delivery map (keyed by jsonl, survives reconnects) BEFORE speak, so the next bind skips with "already fully delivered". Wanted behavior preserved: a reply that lands while disconnected, or is cut mid-stream, still replays exactly once on reconnect/switch-back. **Tests:** 5 pins (speaks once + stamps; second bind silent; stamp-before-speak ordering; cut replays once then locks; stale never stamps). |
| SM-20 | Headless distillations produced NOTHING for 12+ hours: every pass died at the 60s timeout and logged a lying "empty reply" | ✅ shipped, awaiting rebuild | (this commit) | **Evidence:** ~25 `[distill-headless] empty reply for 4bbafb48; skip` lines from 11:28Z to 01:43Z, zero successes; session-end distill calls at 01:36:31/01:42:43/01:42:54 each "emptied" at EXACTLY +60s. **Live probe:** trivial `claude -p` = 26s on this box; realistic 8KB distillation prompt = 57s; real passes exceed 60s. **Root cause:** 60s default timeout in both headless generators, and spawnHeadlessOpus collapsed timeout / non-zero exit / empty stdout into one null so the log blamed a reply that never existed. **Fix:** default timeout 180s (env `DEVNEURAL_DISTILL_TIMEOUT_MS`); spawnHeadlessOpus takes an optional diag callback and names the real failure loudly (TIMEOUT after Nms / exit code + stderr tail / exit-0-empty-stdout); both generators wire it into daemon.log. **Tests:** 4 pins (default constant; both generators hand the default to the spawn seam; explicit override wins). |
| SM-21 | Cold-start preload missed the live conversation log: voice-only turns never reached a fresh Lex session | ✅ shipped, awaiting rebuild | (this commit) | **Operator request:** "last 10 turns from log on dashboard should also be included in cold start preload". **Hole:** the anchor block only rendered turns extracted from prior CC jsonl refs; direct-llm (voice) turns live exclusively in brainstorm_chunks, so a refless voice brainstorm preloaded zero conversation - and with SM-20 killing every distillation, cold starts had nothing at all. **Fix:** buildSiblingIndex appends a "Recent conversation (dashboard log)" block - last 10 user/lex chunks for the anchor, chronological, tool chunks skipped, 400-char snippets - and renders it even when the anchor has no refs (safe under strictScope: chunks are anchor-scoped by construction). Degrades gracefully: when distillation lags, the newest exchange still preloads verbatim. **Tests:** 4 pins (refless voice anchor renders; 10-cap + tool-skip + chronological; ordered after the prior-refs block; no refs + no chunks stays empty). |
| SM-22 | Help page rebuilt as a full help section | ✅ shipped, awaiting rebuild | (this commit) | **Operator request:** "update help page to make it a full blown help section like real software". **Fix:** sticky topic sidebar (anchor nav with scroll-position highlighting via IntersectionObserver, search-aware dimming), anchored section cards with copyable # links, title+body search. Four new plain-English topics under public/help/: getting-started.md (the three pieces, first five minutes), voice-guide.md (one-mouth guarantee, quick layer vs deep brain, replay-once rules, delivery verification, panic phrase), brainstorms-and-sessions.md (what ends a brainstorm and what never does, the flush, end pipeline, cold-start preload verdicts), troubleshooting.md (grep-ready log signatures per failure mode). Content stays runtime-fetched markdown so copy edits need no rebuild. Dashboard 233 passed, tsc clean. |

### Voice engine replacement wave (2026-07-17, VOICE-TOP-LAYER-SPEC)

| # | Title | Status | Commits | Evidence + fix |
|---|-------|--------|---------|-----------------|
| VE-0 | Phase 0 audit: five findings with file:line evidence | ✅ shipped | 14eaded | voice-review.md appendix: AudioContext playback is AEC-blind (the echo source); barge died from per-segment cooldown restamps x 1500ms pref, not one commit; during-TTS window dies at synth end (drain hole); server-side vad_redemption pushes never reach a running VAD; stop-class goes through an LLM or queues to boundary; duplicate delivery = repaste + CR storm + requeue; 12 bell emitters inventoried. |
| VE-1 | Engine core modules (daemon), tests first | ✅ shipped, awaiting rebuild+restart | 7510a7b | echo-filter (fuzzy TTS-vs-transcript discard), barge-word-gate (words interrupt, noise never), endpoint-governor (500ms floor / 3000ms hard ceiling around Smart Turn), interrupt-arbiter (deterministic stop-class + played-ms truncation + rethink-vs-finish policy), delivery-dedupe (one utterance = one delivery). 46 pins. |
| VE-2 | Client playback core: media-element queue | ✅ shipped | a921ca1 | WAV wrapper + segment queue + instant cancel with played-ms + true-drain callback + browser sink (setSinkId). 11 pins. AEC references media elements; the old buffer-source path was invisible to it (Chromium 40504498). |
| VE-3 | Daemon + client wiring: full engine cut | ✅ shipped, awaiting rebuild+restart | 0f04375, d9ec1a7 | New frames (vad-onset / asr-interim / asr-final / playback-drained / playback-stopped); word gate owns the TTS kill; stop-class finals hard-interrupt mid-turn via the existing hold_up primitive with a 5s asr/whisper dedupe latch; echo transcripts drop loudly + resume barged speech; during-TTS window closes at client drain; context truncates to words actually heard; delivery registry gates direct/queue/flush/repaste paths; VoiceClient loses the AudioContext scheduler, the raw-VAD kill, and the barge cooldown gate (net -36 lines). 8 glue pins. |
| VE-4 | DRIVE-QUEUE riders | ✅ shipped, awaiting rebuild+restart | d246c15 | Reinforcement telemetry reclassed conversation (off the bell; correction keeps it); idle_prompt debounced 10min/session (permission stays instant); cross-session injects verify delivery on both transports with CR retries + followup bell on stuck paste; reminders emitter already action-required-only; deck nesting landed earlier (4779d7b). 15 pins. |
| VE-5 | Held-turn governor flush: hard ceiling ships starved mid-thought fragments | ✅ shipped, awaiting restart | 4dfe56b | **Audit correction:** Smart Turn v3 was already fully integrated (smart-turn.ts, 777 lines: onnxruntime-node inference, sha256-verified auto-download, runtime flag) with the 8.3MB model on disk since 07-15 and holding/merging live in tonight's log (02:50Z, 03:05Z) - no download pending. Same for deck nesting: live on master since 4779d7b (StreamDeck.tsx:239-254, data-testid deck-nested-worker). **The real gap:** decideCoalesce is timer-free, so a held fragment whose speaker never resumed starved forever. Fix: the hold path arms an endpoint-governor flush loop (200ms cadence, 3000ms hard ceiling) that pops the held words and ships them through processTurnText - the post-endpointing tail extracted from handleUtteranceEnd so a flushed fragment takes the identical pipeline. 3 composition pins. |
| VE-6 | "Spoke to Lex, nothing happened, mirror never started" (13:25Z): warmup probe swallowed by a slow boot; bare-CR nudges cannot resurrect it | ✅ shipped, awaiting restart | 0485e13 | **Evidence:** daemon.log 13:25:45Z spawn (ptyId=a1e8a912, ccSessionId=6d6e15ba) -> 13:30:18Z `WARMUP FAILED: no assistant reply and all liveness signals quiet`; no 6d6e15ba jsonl anywhere; the terminal ring (GET /sessions/6d6e15ba.../terminal-replay) shows claude booted to a fully rendered EMPTY composer - the probe text never arrived. Direct ConPTY repro: probe injected after first paint echoes + replies + writes jsonl with the identical env/cwd, so only the pre-paint inject window loses text (cooked->raw ConPTY transition discards it; the box was in a restart storm, first paint outran the 3s boot delay). **Root cause:** warmup injects the probe once at t=3s blind, then re-nudges bare CRs only - CRs into an empty composer are no-ops forever. **Fix:** while the session jsonl has never grown, re-inject the FULL probe (commit=true) every 15s (`WARMUP_REPROBE_MS`); a committed probe writes its user record immediately, so jsonl growth gates the re-probe off; duplicated composer text still commits one garbled turn and any reply warms. **Tests:** 4 pins (swallowed probe re-sends full text and still dies if never answered; re-sent probe rescues warmup at 20s; fast boot sees exactly one probe; existing renudge pin green). |
| VE-7 | Daemon ran a whole day with repo-root cwd + a live CC session's env: hook lazy-spawn won the restart race with no hygiene | ✅ shipped, awaiting restart | 0485e13 | **Evidence:** 13:22:32Z `/admin/daemon/restart` armed the schtasks relauncher; old daemon exited 13:22:38.3Z; pid 135480 booted 13:22:38.9Z with `[voice-brain] ... cwd=C:\dev\Projects\DevNeural` (every prior spawn: `cwd=...\07-daemon`). start-daemon.ps1 always sets `-WorkingDirectory` to 07-daemon, so the winner was ensureDaemonRunning (lifecycle/spawn.ts) fired from a Claude Code hook during the gap - it passed NO cwd and the RAW hook env (CLAUDECODE, CLAUDE_CODE_*, CLAUDE_TRANSCRIPT_PATH, VSCODE_*, ELECTRON_RUN_AS_NODE all leak; verified live in this session's env). Wrong cwd silently moves the voice-brain transcript slug; the leaked env is the same hazard class as the 2026-07-09 child-session bug, one hop earlier. **Fix:** lazy spawn now passes `cwd: daemonPackageRoot()` and `sanitizeDaemonEnv(process.env)` (strips CLAUDE*/VSCODE_*/ELECTRON_RUN_AS_NODE, keeps CLAUDE_CONFIG_DIR + ANTHROPIC_* + DEVNEURAL_*), so both launch paths hand the daemon the same root + environment. pty-host's sanitizeClaudeSpawnEnv stays as defense in depth on the next hop. **Tests:** 4 pins (package root; identity/IDE strip; config-scope keep; env holes). NOTE: the CURRENTLY RUNNING daemon still has the wrong cwd/env - the next operator restart via the task/script clears it. |

Operator verification pending (live, after restart - restart is the
user's call): speech cuts Lex audio fast (word gate: expect one spoken
word of latency, not noise-instant); a long reply produces zero phantom
utterances; mid-thought pauses never ship fragments (Smart Turn bounds);
"stop" lands mid-turn under a second (asr fast path); one utterance =
exactly one delivery; bell stays quiet through a full conversation.

## Fix 1-13+ work cycle (2026-05)

| # | Title | Status | Commits | Notes |
|---|-------|--------|---------|-------|
| 1 | Voice settings auto-resync on daemon ws reconnect | ✅ | d31bfd2 | |
| 2 | /health audio block + ok=false on orphaned session | ✅ | ca05284 | |
| 3 | smart-compact wait for new session SessionStart | ✅ | 2a9fda9 | |
| 4 | Dashboard copy rewrite (7 pages + Mac glyph sweep) | ✅ | 289940e, 462aa95, 9e0ccc2, 44406e1, 839d17c, 34fa555, b55c048 | |
| 5 | Docs cleanup (7 items) | ✅ | 3002983, 1c597d7, 0f9aa8e, dcceed3, 52480a3, e8287c6, dbd8cfb | |
| 6 | Responsive top-bar + supervision chips segmented | ✅ | 888ca50, 525e707, 80589cb | Voice-pill icon-only restructure at <480px still needs in-browser sweep. |
| 7 | Six-section resume builder | ✅ | 8d34148 | |
| 9 | Notifications classifier + bell filter | ✅ | 4a0c7c4 | |
| 10 | Wake-word audit + standby/listen + voice-commands.md | ✅ | ec614a6, a06c838 | |
| 11 | Dashboard Help page scaffold + content | ✅ | 927b88b, bfa0d1b, 488e5f6 | |
| 12 | Feedback memories baked into Lex system prompt | ✅ | 2eb63b7 | |
| 13 | TTS speak on stop_reason=tool_use (pre-tool ack) | ✅ | 873a7c2 | Smoke verified live 2026-05-17 ~10:07 EDT (iPad client). |
| 14 | Neural-network legend anchored to panel + mobile safe area | ✅ | a9b2595 | Wrapper now `100dvh`-based + mobile-tab-bar aware; legend flowed in flex column with `env(safe-area-inset-bottom)` pb. |
| 15a | Cross-session-inject: anchor-resolved dispatch + auto-redirect | ✅ | 6c2f8c2 | migration 029, previous_session_id, redirect/dormant audit rows, resolveAnchorDispatch helper. |
| 15b | Cross-session-inject: anchor_id-signed HMAC alternate | ✅ | aef80dc | verifyToken multi-subject, route accepts signed_anchor_id, /auth/cross-session-token gains anchor_id mode. |
| 15c | Smart-compact: replay parked injects on resume | ✅ | 57c5304 | migration 030 payload_text col, findParkedInjectsForAnchor, fireSmartCompact replays max 3 before summary. |
| 16a | deck-hook tolerate missing tmp file (defense) | ✅ | stream-deck 83f46c7 | `[ -f "$TMP" ]` guard stops the `mv: cannot stat` flood on the CC UI immediately. |
| 16b | GET /projects/anchors/by-session/:uuid (DevNeural side) | ✅ | dc8f41a | additive endpoint for external state-keying consumers (deck-hook, future supervisors). |
| 16c | deck-hook anchor-keyed state + race-safety | ✅ | stream-deck 77db4c2 | bounded retry on daemon lookup, pid+nanos TMP suffix for concurrent hooks, AnchorId/RecordedSessionId in payload. |
| 16d | one-shot migration of legacy uuid-keyed state files | ✅ | stream-deck 505c43d | mtime>60s guard for Race 3, marker file gate at `.migrated-fix16`, content-idempotent. |
| 16e | virtual deck source change | ⏭ skipped | — | Per amendment 2 conditional: virtual deck reads `/sessions` endpoint, not state files; layout change is transparent. No commit needed. |
| 17 | TTS barge-in regression (watchdog desync) | ✅ | d6f094a | resetTtsPlayback now clears ttsActiveRef and stamps lastBufferProgressTsMsRef so the c2335c5 watchdog stops misreading post-barge-in as buffer_stuck. Dashboard React, no rebuild needed. |
| 18 | Cross-session-inject deliverability gate | ✅ | f9ce914 | Presence payload gains has_terminal_for_uuid; filename gains __<bridge-id> so multi-window same-cwd does not clobber. Bridge halts offset on [skip]. Daemon resolveDeliverableBridgeForSession returns deliverable / legacy-grace / no_terminal / not_claimed. crossSessionInject short-circuits with decision='no_deliverable_bridge' when no bridge has a terminal. Migration 032 widens decision CHECK. Absorbs cold-start-blind bug. Rebuild + restart required for daemon + bridge VSIX. |
| 19 | Voice-mode inject missing CR (regression) | ✅ | 6195466 | ptyInject (pty-host.ts:754) now writes body+\r atomically in one pty.write call when commit=true, replacing the prior 80ms setTimeout that raced ahead of the bracketed-paste close terminator. A second bare \r nudge fires ~1s later per the durable user rule (belt-and-suspenders, idempotent). buildPtyInjectPayload extracted as a pure helper; tests/pty-inject-payload.test.ts pins the contract. Voice WS dispatch picks up the fix transparently (state.bindKey targets the daemon-owned Lex PTY; bridge fallback was already wired in Fix 18 for the cross-session-inject hop). Rebuild: yes (07-daemon dist regen). |
| 20 | Voice barge cancels Lex mid-turn (critical) | ✅ | a00cea6 | killActiveTts step 3 (PTY Ctrl+C) now lives inside the same `if (ctx)` block as steps 1+2, so the abort only fires when TTS was actually playing. New state.pendingUserUtterances queue captures mid-turn-no-tts utterances; handleUtteranceEnd pushes them and returns `{t:'queued-mid-turn'}`. handleJsonlLine calls flushPendingUtterances() the instant Lex's end_turn lands (awaitingResponseSince clears on a non-pre-tool-ack record), shipping the deferred follow-ons as one combined `[voice-context: queued-mid-turn-utterances (N)]` inject. Preserved unchanged: TTS-cancel + partialChain (interrupted-replies pipeline), voice-command precedence, AEC-residual gate, direct-llm runtime mode. Rebuild: yes (07-daemon dist regen). |
| 21 | Push respects notify_class taxonomy (Fix 9 follow-up) | ✅ | e61f315 | push.ts maybePushNotification gate matrix updated to honor Fix 9 notify_class. New policy: conversation -> skip (matches bell filter), report -> send (end-of-session / handover), followup -> send (action-required), signal + severity=info -> skip, signal + severity>=warn -> send. Legacy rows without notify_class default to conversation (safer, matches bell filter default). mode=force preserves the conversation skip so class beats mode. End-of-session emit added to runSessionEndPipeline wrapper with notify_class='report' + source='session-end' so the bell + phone push surface the wrap on every primary-runner path (voice "end session", PTY exit, idle-watcher day-cap, admin redistill). Rebuild: yes (07-daemon dist regen). |
| 22 | First-utterance silent TTS after fresh brainstorm session start | ✅ | 7bc5c76 | **Symptom:** user starts new brainstorm, speaks hello, Lex generates assistant-text frame normally, TTS audio never plays for first turn; subsequent turns play fine. **Path note:** task spec says "06-dashboard"; actual dir is `08-dashboard`. **Root cause:** AudioContext lifecycle is double-keyed and the two keys drift apart on session change. Creation lives ONLY in the user-gesture click handler `toggleEnabled()` at `08-dashboard/components/VoiceClient.tsx:2667-2687` which calls `warmAudioContext` (`08-dashboard/lib/voice-audio-warm.ts`). Teardown lives inside the enable effect at `08-dashboard/components/VoiceClient.tsx:1545-1614`; teardown closes the ctx and nulls `audioCtxRef.current` at lines 1605-1613. **Effect deps are `[enabled, sessionId, mode]`** (line 2624). When the user creates a new brainstorm, `sessionId` changes (derived from `lexPty?.sessionId` at line 311). React fires cleanup, teardown nulls the AudioContext. Effect re-runs, opens fresh WS, sends `hello`, gets `hello-ack`, starts VAD — **never re-warms `audioCtxRef`**. ctx stays null. **First tts-start** at `08-dashboard/components/VoiceClient.tsx:1944-2001` deliberately does NOT lazy-create (comment at 1956-1963 says so; iOS Safari autoplay policy was the prior fix). `playheadRef = ctx?.currentTime ?? 0` = 0. **Binary PCM** routes to `schedulePcmChunk` at line 1109; first lines are `const ctx = audioCtxRef.current; if (!ctx) return;` at 1115-1116 — every frame silently dropped → first reply has no audio. **Subsequent turns recover** because the voice watchdog at line 1210 detects the stall (no buffer progress because the bump at line 1135 was never reached). Heal step 2 at line 1233 calls `resetVoiceAudio()` (lines 1162-1201) which DOES call `warmAudioContext` at line 1184. AudioContext live; next tts-start succeeds. Watchdog interval ~10s = "first reply silent, second reply fine". **Evidence:** (a) asymmetry — only `toggleEnabled` warms, only effect cleanup nulls. (b) deps include sessionId so brainstorm change tears down. (c) ctx-null bail at `schedulePcmChunk:1116`. (d) watchdog `resetVoiceAudio` rewarms. (e) `voice-audio-warm.ts:1-13` explicitly warns about gesture-bound creation; prior fix moved creation into click handler but did not handle sessionId-change teardown. **Fix not yet shipped** (diagnosis-only per task scope). Candidate fix shapes: (1) warm AudioContext inside the enable effect when `audioCtxRef.current === null` (Chromium persistent-activation flag means an earlier gesture in the page lifecycle is sufficient); (2) move ctx close out of teardown so it survives sessionId changes (only close on `!enabled`). Recommend (2) — keeps the original "create only inside gesture" invariant intact and matches the "VoiceClient mounts once at app root, survives navigation" comment at line 287-294. |
| 23 | VAD redemption window max 3s -> 6s | ✅ | cef6fb4 (daemon side) + 7bc5c76 (dashboard slider hunk bundled into Fix 22 commit) | Daemon MAX_VAD_REDEMPTION_MS + dashboard VAD_REDEMPTION_MAX both bumped to 6000. MIN (200) + DEFAULT (768) unchanged. Slider tooltip already warns operator that a value change requires a voice-off / voice-on cycle (silero VAD does not accept live updates). Bundled with Fix 22 per operator directive so a single daemon+dashboard rebuild covers both. Rebuild: yes (daemon + dashboard). |
| 24 | Mid-reply TTS truncation | 🔍 diagnosed | — | **Symptom:** Lex finishes generating multi-sentence text reply, TTS begins, audio cuts off mid-sentence. Not headphones / not self-loop. **Leading root cause:** voice watchdog `resetVoiceAudio` fires mid-reply and truncates the queued audio. Evidence chain: (a) Watchdog interval at `08-dashboard/components/VoiceClient.tsx:1237-1306` runs `runWatchdogChecks` from `08-dashboard/lib/voice-watchdog.ts:55-86` every ~10s. (b) The `ctx_state` check at `voice-watchdog.ts:64-67` is **unconditional**: `ok: state.ctxState === "running"`. Any `suspended` reading flags fail. Chromium aggressively suspends AudioContexts under power throttling, tab-blur, or even brief CPU pressure. (c) On consecutive fail the heal escalates: step 1 = `ctx.resume()` (`VoiceClient.tsx:1226`); step 2 = `resetVoiceAudio()` (`VoiceClient.tsx:1233`). (d) `resetVoiceAudio` at lines 1162-1201 **bumps `ttsGenRef.current` at line 1175** AND clears `activeSourcesRef` AND closes the old ctx. (e) `schedulePcmChunk` at line 1110 bails when `gen !== ttsGenRef.current` — every in-flight PCM chunk from the still-streaming reply gets dropped. (f) Already-scheduled `src.start(...)` nodes get hard-stopped on the closed ctx; un-played tail audio is gone. **Net effect:** Lex was mid-reply, watchdog saw a momentarily-suspended ctx, ran resetVoiceAudio, the second half of the audio queue evaporated, user hears truncation. **Why this hasn't bitten earlier consistently:** Fix 22 (commit 7bc5c76) preserves the ctx across sessionId changes, which leaves the SAME ctx running longer than before, increasing the chance Chromium suspends it for any reason. Pre-Fix 22 the ctx was being recreated frequently so the suspended-then-resume window was tighter. **Recommended fix:** (1) gate the `ctx_state` check on `state.ttsActive` like the other two checks at lines 72-83 (idle suspended state is normal and should not heal); (2) add a one-tick debounce so a single suspended reading does not trip step 1+2 escalation; (3) in `resetVoiceAudio` consider preserving `activeSourcesRef` and NOT bumping `ttsGenRef` when the reset is healing-driven (only bump on user-initiated reset or watchdog-dead banner click) — the gen bump is for cancellation, not for context refresh. Investigation only; no fix shipped this cycle. |
| 25 | Mic input level + mic sensitivity sliders not applying or scaled wrong | 🔍 diagnosed | — | **Symptom:** mic input level ~0.55x AND mic sensitivity 5/100 (well below default), Lex still picks up background speech. **Root causes (two distinct):** **(A) silero VAD requires voice-off/on to apply sensitivity changes.** `08-dashboard/components/VoiceClient.tsx:486-489` comment: "silero VAD does not support live threshold updates". `vadSensitivityRef` is read ONCE at VAD init at line 2455-2459 and baked into the silero instance. Slider change updates the ref but the running VAD instance keeps the old thresholds until the operator cycles voice. Tooltip is supposed to warn but the operator's report suggests they were unaware. **(B) Scaling mismatch / mapping curve is too permissive.** `vadThresholds(s)` at `08-dashboard/components/VoiceClient.tsx:249-257`: `positive = 0.7 - 0.4 * s`. UI range is 0..1 (`VAD_SENSITIVITY_MIN=0`, `VAD_SENSITIVITY_MAX=1` at lines 205-206); UI slider may render 0..100 but the stored value is normalised to 0..1. At "sensitivity 5/100" (s=0.05), `positive = 0.7 - 0.02 = 0.68`. silero opens speech at p>=0.68 confidence — still relatively permissive. Operator's intent ("don't pick up background noise") needs `positive >= 0.9`, which would require s = -0.5 (out of UI range). The current mapping cannot produce strict thresholds at any slider position. **(C) mic_gain only affects the audio SENT to whisper, NOT silero detection.** `micGain` is applied at three sites: `08-dashboard/components/VoiceClient.tsx:2198` (parallel capture buffer), 2247 (silero `onSpeechEnd` audio → STT ship), 2526 (push-to-talk buffer). silero VAD reads the RAW MediaStream upstream of any gain application; lowering `mic_gain` to 0.55x just attenuates what whisper sees after silero already decided to open the gate. If silero opens on background noise (per (B)), whisper will still get a quiet-but-recognisable signal and may transcribe it. **Verification done:** confirmed micGain reaches whisper via finalizeUtterance at line 2247. Confirmed vadSensitivity reaches silero at line 2455-2459 ONLY at MicVAD.new() time. **Recommended fix:** (1) widen vadThresholds mapping so s=0 produces `positive >= 0.9` (e.g. `positive = 0.95 - 0.65 * s` gives 0.95 at s=0 and 0.3 at s=1); (2) surface a clearer "voice off/on required" inline toast when the VAD slider changes mid-session, not just in the tooltip; (3) apply micGain BEFORE silero's stream input via a MediaStream GainNode so attenuated audio also reduces VAD's perceived volume — this turns mic_gain into a true noise floor knob. Investigation only; no fix shipped this cycle. |
| 27 | Voice mic-init OOM recurrence (mobile Safari) | ✅ | f237673 | VoiceClient.tsx mic-init catches (VAD + push-to-talk) now call `resetVadModuleCache()` BEFORE setStatus/setErrMsg so Retry boots a fresh ORT env (no "previous call to initWasm() failed" cascade) AND emit `logVoice("vad-error", ...)` so the failure lands in the ring buffer + Voice diagnostics panel (was UI-toast-only, invisible to operator + audit). Bug doc: 2026-05-16-voice-restart-oom-regression.md (2026-05-25 recurrence section). Fix 3 (lazy-load Whisper on non-isolated tabs) deferred until short-term fixes prove insufficient on repeat hits. Bug doc stays open until 24h observation passes. Dashboard rebuild required (Next.js static export). **Smoke verified (code-path) 2026-05-26 02:25 EDT:** `logVoice("vad-error", ...)` wired at VoiceClient.tsx:2483 + :2588 inside both mic-init catches (VAD + push-to-talk). Live ring-buffer capture deferred to desktop session next OOM occurrence (logged in TODO.md Validation). |
| 28 | Brainstorm jsonl repoint can drop trailing turns | ✅ | (this commit) | Two changes in one patch. **(1)** `brainstorm-store.ts:bindBrainstormSessionId` now runs one synchronous `runBrainstormJsonlIngestTick` against the row in its CURRENT state BEFORE flipping `claude_session_id`. Drains any turns appended to the OLD jsonl between the last cron-cadence ingest tick and the repoint, with the correct OLD `cc_session_id` stamped per line. Guarded against drain failures (best-effort; never blocks the bind). **(2)** `brainstorm-jsonl-ingestor.ts` offsets map keyed by `${rowId}:${claude_session_id}` (composite) instead of `rowId` alone. After a repoint, the new key starts at offset 0 cleanly; old key remains in the map but is no longer referenced. Prevents stale-offset seek into the new jsonl (which would either return empty or skip into the middle of the new session's content). Bug doc: 2026-05-25-jsonl-repoint-drain-loss.md. Three contract tests in tests/jsonl-repoint-drain-loss.test.ts pin the repoint, stale-offset, and drain-on-repoint scenarios. Daemon rebuild required. **Smoke verified live 2026-05-26 02:11 EDT:** worker 837dd156 -> /clear -> new session 94e85826 spawned cleanly; project_session previous_session_id preserved 837dd156; old jsonl frozen at 02:09, new jsonl active at 02:11. No trailing turn loss observed. |
| 31 | Voice mode first-turn TTS silent on every fresh Lex spawn | ✅ | (this commit) | **Symptom:** open fresh brainstorm, toggle voice, speak first utterance, reply renders as text but never plays as TTS; turn 2 onward plays normally. Closes docs/bugs/2026-05-14-no-tts-on-first-prompt-after-restart.md (2026-05-24 recurrence section). **Root cause (two-layer race, proven via daemon.log probes):** (1) `08-dashboard/components/VoiceClient.tsx:2671` voice-engine effect deps were `[enabled, sessionId, mode]`. On fresh PTY spawn, `lexPty.sessionId` starts null (PTY exists, Claude Code has not emitted SessionStart yet). WS #1 opens with `session_id=undef`; daemon `bind()` falls back to PTY-cwd discovery and stamps `state.bindKey = ptyId`. User speaks; `handleUtteranceEnd` injects and sets `state.awaitingResponseSince`. Claude Code processes the inject, finally emits its cc-session-id. ~1.5-3s later the dashboard's `/pty-list` poll returns the populated `sessionId`, React state changes, effect deps change, effect re-runs, cleanup closes WS #1. (2) WS #2 opens with `session_id=<resolved>`. Fresh `ConnState`: `awaitingResponseSince = 0`. `bind()`'s session-id branch at `07-daemon/src/voice/lex-voice-ws.ts:419` finds the jsonl and stamps `jsonlOffset = fs.statSync(jsonl).size` (EOF). Claude Code finishes the assistant turn and writes it past that offset. `pollJsonl` reads the new bytes; `handleJsonlLine`'s gate at line 762 (`if (!readOnly && !state.awaitingResponseSince) return;`) drops the record because WS #2 never saw an inject. Probe trace confirmed verbatim: WS #1 hello session_id=undef → bind via PTY-cwd → inject + awaiting stamped → WS #2 hello with resolved cc-session-id ~20s later → EOF offset stamped → `handleJsonlLine GATE drop: type=assistant awaiting=0`. **Fix (two layers, both shipped here):** **Layer 1 — daemon, the structural invariant.** `bind()` now migrates `awaitingResponseSince` from the brainstorm row. After the bind resolves a handle, look up the brainstorm via `handle.sessionId` or `handle.ptyId`. If `lifecycle_state === 'speaking'` a turn is in flight; stamp `state.awaitingResponseSince = Date.now()` and start the watcher if not already running. The brainstorm row is the source of truth — set on every inject (`handleUtteranceEnd:1900-1910`) and cleared at the matching end_turn (`handleJsonlLine:818-822`). Inheriting from it makes the gate correct for any WS replacement cause: sessionId-resolve, smart-compact restart, daemon restart, multi-tab eviction. **Layer 2 — client, identity correctness.** Effect deps in `VoiceClient.tsx:2671` change from `[enabled, sessionId, mode]` to `[enabled, lexPty?.ptyId ?? null, mode]`. `ptyId` is the true PTY identity; it does not change when Claude Code resolves its cc-session-id. The tuple now only changes on events that justify a WS replacement (brainstorm switch, manual Lex restart). Smart-compact restart swaps `bindKey` + `jsonlPath` in-daemon without changing client-visible `ptyId`, so this dep also avoids redundant churn there. **Verification:** real-hardware test post-rebuild: fresh brainstorm, voice on, first utterance, first reply spoken. 863/863 daemon tests green. Probes stripped before commit. Rebuild: yes daemon TS + dashboard Next.js static export both required. |
| 30 | Voice WS late-jsonl-resolve offset stamp lands past assistant text | ✅ | b3827e9 | Defensive hardening for the jsonl polling watcher. When `bind()` could not find the jsonl (PTY just spawned, file not yet created), `pollJsonl`'s late-resolve branches stamped `jsonlOffset = fs.statSync(jsonl).size` on first discovery, which can land past any assistant text already written between PTY spawn and the late-resolve tick. Changed both late-resolve branches in `07-daemon/src/voice/lex-voice-ws.ts:632-682` to set `jsonlOffset = 0`. Safety: the `awaitingResponseSince > 0` gate (line 762) + the `rec.type !== 'assistant'` filter (line 755) + the `spokenSegmentHashes` dedupe (line 793) already filter out pre-bind content; reading from 0 only admits content that landed after the matching inject. Same shape as the 2026-05-14 compaction-restart bind at line 1055. Note: the production bug originally attributed to this fix turned out to be Fix 31 instead; this change remains a correct defensive improvement to the late-resolve path. Daemon rebuild required. |
| 29 | Hold-up leaves Lex stuck in thinking until browser refresh | ✅ | (this commit) | `runHoldUp` Ctrl+C aborts the Lex PTY mid-turn, so no `end_turn` jsonl record lands to clear `state.awaitingResponseSince` at `07-daemon/src/voice/lex-voice-ws.ts:746`. The next user utterance hits the mid-turn-no-tts gate, gets pushed to `pendingUserUtterances`, and emits `queued-mid-turn` instead of injecting. The flush point is gated on the same `end_turn` that never arrives, so the queue is permanently stuck. Fix: after the `runHoldUp({...})` call in the `hold_up` dispatch (lex-voice-ws.ts:1512-1565), clear `state.awaitingResponseSince = 0`, drain `state.pendingUserUtterances`, and `spokenSegmentHashes.clear()` inline. The aborted turn's queued utterances were context for a turn the user just redirected away from, so replaying them into the next inject would be wrong; clearing them keeps the next utterance clean. Two new tests in `tests/lex-voice-hold-up.test.ts`: one behavioral pin asserting the three resets land, one wiring pin reading the dispatch source so a future refactor cannot silently regress. Daemon rebuild required. |
| 32 | Mid-turn flush queues utterance but never submits | ✅ smoke-verified 2026-05-26 03:23 EDT (SMOKE Step 3.5 PASS, queued mid-turn utterances auto-submit at turn boundary) | (this commit) | **Symptom:** mid-turn-no-tts queue flushes the deferred utterance into the worker's input field but the worker stays idle; cursor sits after `[voice mode] ` and the inject never submits on bridge-attached workers. **Root cause:** `flushPendingUtterances` at `07-daemon/src/voice/lex-voice-ws.ts:1440` called `ptyInject(state.bindKey, body + voiceTag, true)` and returned. Only the primary atomic write + the 1 s commit nudge inside `ptyInject` (`pty-host.ts:801-814`) cover the CR. On bridge-attached workers that one nudge is observably insufficient — same class of bug the cross-session-inject path already mitigates by firing an explicit second bare CR ~850 ms after the primary write (`07-daemon/src/lex/cross-session-inject.ts:297-306`). **Fix:** extracted `flushPendingUtterances` body into the exported `_flushPendingUtterancesImpl` test seam at module scope. After a successful primary `ptyInject(..., commit=true)`, schedule a bare-CR follow-up via `setTimeout(..., 850)` (matches `DEFAULT_COMMIT_DELAY_MS` in cross-session-inject) that fires `ptyInject(state.bindKey, '\r', false)`. `commit=false` so ptyInject does not append a second CR onto a bare CR. Belt-and-suspenders per the durable user rule: every inject must be followed by a bare CR ~1 s later; no race-classification, no downside. Regression test in `tests/lex-voice-ws-flush-cr.test.ts` pins the contract: exactly two `ptyInject` calls (first body+commit, then bare CR no-commit); follow-up is skipped when the primary fails. Rebuild: yes daemon TS — flushPendingUtterances changed. |
| 33 | Lex stuck after tool cancellation; only recovers on next external user message | ✅ smoke-verified 2026-05-26 03:23 EDT (SMOKE Step 3.6 PASS, cancelled-tool-recovery armed + self-heal observed in daemon log) | (this commit) | **Symptom:** any cancelled tool call (user text preempting mid-tool, hook denial, network hiccup, CC-side reject envelope) leaves Lex with no follow-up turn. The cancellation closes the turn but Lex does not auto-resume; the next user message has to manually unstick the loop. **Fix:** new daemon-side recovery primitive `07-daemon/src/lex/cancelled-tool-recovery.ts`. Tails every active brainstorm's CC jsonl on a 5 s cadence (composite `${brainstormId}:${ccSessionId}` offset map matching the brainstorm-jsonl-ingestor's repoint-safe scheme). When a `user` line carries a `tool_result` whose text matches the canonical CC reject patterns (`/The user doesn't want to proceed with this tool use/i` or `/Request interrupted by user/i`), the session is armed. Any new assistant line within 5 s clears the arm (Lex self-recovered). If no assistant follow-up lands, a single recovery inject fires via `crossSessionInject` (HMAC token + commit=true + bare-CR follow-up already baked into that path; caller_label=`lex-cancelled-tool-recovery`). The recovery body is imperative — "[recovery] A tool call was cancelled. Treat as transient interruption, not a deliberate reject. Resume where you left off..." — and tells Lex to act on best judgment without asking the user. **Safety reasoning:** the reject envelope already ENDED the turn, so the daemon-fired continuation is a fresh user turn, not a preempt. **Two-strike escalation:** a second reject envelope landing inside 30 s of the prior recovery firing is the recovery itself getting cancelled. The detector emits a `recovery_exhausted` audit row (`decision='shadow'` + `reject_reason='recovery_exhausted: ...'` + `caller_label='lex-cancelled-tool-recovery'`, written through the existing `cross_session_injection_log` table; no new table) and a `t:'recovery-exhausted'` WS frame via a new `broadcastRecoveryExhausted(bindKey, reason)` exported from `lex-voice-ws.ts`. No second auto-fire; the dashboard banner is the recovery path for the user. **Debounce:** two cancellations inside the 5 s window stay armed against the first stamp so a flurry of CC retries surfaces as exactly one recovery inject. **Wiring:** `startCancelledToolRecovery` is booted in `daemon.ts` alongside the brainstorm-jsonl-ingestor; the handle is captured in `cancelledToolRecoveryHandle` and torn down in the shutdown closure. Four contract tests in `07-daemon/tests/cancelled-tool-recovery.test.ts` pin the happy-path single fire, the self-heal skip, the two-strike exhaustion, and the within-window debounce. The recovery layer sits BELOW mid-turn text-input routing, the voice queue, and ptyInject — all out of scope per the spec. Rebuild: yes daemon TS — new background service wired in daemon.ts. |
| 34d | Lex CC targets always hit no_deliverable_bridge | ⚠️ regressed (superseded by 34d.1) | 2e0d590, 2026-05-26 01:44 EDT | **Original symptom:** post Fix 34b+34c the wire fired end-to-end and `resolveLexTargetSession` picked the correct Lex cc, but every inject landed `decision='no_deliverable_bridge'` in `cross_session_injection_log`. **Original (regressed) fix:** branched `buildInject` to call `queueSessionPrompt(ref.lex_session_id, text)` when the target was a Lex cc; otherwise fell through to `crossSessionInject`. **Regression discovered 2026-05-26 02:30 EDT:** `queueSessionPrompt` is bridge-keyed by CC session id (writes `<sessionId>.in` for the 09-bridge VSIX to paste). Keying on `lex_session_id` (a brainstorm UUID) means no VSIX terminal matches; the bridge fell back to the only terminal in scope, which was the WORKER. Supervisor-event payloads landed in the worker's input queue while the audit row reported `delivery_mode=lex-queue accepted`. Concrete evidence: worker jsonl 94e85826 with `[supervisor-event] worker=DevNeural event=idle…` content in queue-operation rows; cross_session_injection_log row id=… target_session=2a708d6d (Lex CC) decision='accepted'. Audit lied; live delivery hit the wrong terminal. **Superseded by Fix 34d.1** (this commit, 2026-05-26). |
| 60 | distillation dump-to-markdown helper | ✅ | (this commit), 2026-06-01 | **Symptom:** distillations live in `brainstorm_sessions.last_summary` + `last_summary_ms` columns in `index.db`. No way to skim them outside the dashboard. The 2026-05-14 brainstorm captured the gap; the row sat open as "Open: build a quick dump-to-markdown helper". **Fix:** new CLI `07-daemon/scripts/dump-distillations.ts` wired as `npm run dump-distillations`. Args: `--limit N` (default 20), `--out path.md` (default `C:/tmp/distillations-YYYY-MM-DD.md`). Read-only against the live DB; honours `DEVNEURAL_DATA_ROOT`. Writes a single markdown file with one section per row containing label + id + status + timestamps + the full last_summary body. **Smoke:** `npm run dump-distillations -- --limit 5` returned `wrote 5 rows`; markdown rendered correctly with the Fix 54 jsonl-fallback distillations the boot recovery sweep produced. No tests required: read-only script with no side effects on persistent state. Rebuild: not required (CLI script). |
| 59 | TTS sanitizer (server-side enforcement of voice-mode rules) | ✅ | (this commit), 2026-06-01 | **Symptom:** Lex's voice contract said "no full paths, no markup chars, no UUIDs as numbers" but the rule lived only in the system prompt + the operator's memory file. Slips leaked through to the speaker (paths read character-by-character, UUIDs spoken digit-by-digit, JSX braces voiced). **Fix:** new module `07-daemon/src/voice/tts-sanitize.ts` exports `sanitizeForTts(text)`. Pure + idempotent. Rules in order: (1) URLs to host only (`https://github.com/x/y` -> `github.com`); (2) Windows drive-letter paths to basename (`C:/dev/Projects/DevNeural/foo.ts` -> `foo.ts`); (3) POSIX absolute paths to basename, requires >=2 segments after leading slash to avoid eating "/var" alone; (4) UUIDs + hex digests >=32 chars -> "opaque id"; (5) angle-bracket markup stripped; (6) whitespace runs collapsed, trailing whitespace trimmed per line. URL rule runs FIRST so the Windows path regex does not mistake `https://...` (the `s:` looks like a drive letter) for a path and shred the URL before the URL rule sees it. Wired into `07-daemon/src/voice/piper.ts:synthesize` immediately before `proc.stdin.write` so every TTS string passes through, regardless of upstream source (Lex assistant text, smart-compact ack, voice-cmd reply). **Tests:** 14 new pins in `tests/tts-sanitize.test.ts` (empty input; Windows forward + backslash paths; POSIX paths; URLs with port + query; UUIDs; long hex digests; short hex preserved; angle-bracket markup; whitespace collapse; idempotency; natural speech preserved; combined rules in one pass). 1123/1123 daemon tests pass (was 1109). tsc clean. No migration. Rebuild: yes (07-daemon dist regen). |
| 57 | COALESCE Phase B + C: classifier, conflict push-back, AbortController, text-input WS frame | ✅ | (this commit), 2026-06-01 | **Symptom:** COALESCE Phase A (Fix 35) shipped the queue + drain primitives but left B/C deferred. Mid-reply utterances arrived at Lex as a raw numbered list with no kind tags, so deterministic composition (follow-up extends vs new switches topic vs noise drops) was on Lex's reasoning rather than the daemon. A contradiction utterance cancelled the queue but did NOT abort the in-flight ollama call, so the cancelled instruction still landed as one more assistant chunk after "never mind". Typed input had no equivalent frame at all: the dashboard chat surface had to live in a separate path. **Phase B (Fix 57.A):** `07-daemon/src/voice/lex-voice-coalesce.ts` gains four pure functions. `classifyUtterance(text, ctx)` returns `{text, kind}` where kind is `follow-up` / `new` / `noise` / `cancel`. Heuristic ordering: blank/filler -> noise; contradiction patterns -> cancel; lead-in connectors (and/also/oh and/actually/add) -> follow-up; tail-overlap of >=2 content tokens with prior queue or in-flight reply -> follow-up; else new. `detectRuleConflict(text, rules)` returns `{hit, rule?}` for the first matching durable rule, with rules passed in by the caller (memory-store plumbing deferred). `formatQueueDrainV2(items, opts)` is the structured drain: drops noise items, short-circuits to the latest cancel when present, prepends a `[voice-context: conflict]` block when any item hits a passed rule, otherwise renders the numbered list with `[kind]` tags so Lex's composition is deterministic. **Phase B (Fix 57.B AbortController):** `lex-voice-ws.ts` ConnState gains `directLlmAbort: AbortController | null`. `handleDirectLlmUtterance` binds a fresh controller before each `callVoiceChat` call (the helper already accepted `signal` via `VoiceChatOptions`), clears it in a `finally`. The contradiction handler at the queue-push site calls `directLlmAbort.abort()` so ollama stops generating immediately. Returns an `AbortError` which the call site treats as `direct-llm-aborted` (no chunk write, no speak, WS frame `t:'direct-llm-aborted' reason:'contradiction'`). **Phase C (Fix 57.C text-input):** `lex-voice-ws.ts` adds a `t:'text-input'` case that flows typed text through the same `pendingUserUtterances` + `runDirectLlmCoalesceLoop` pipeline voice uses. Skips voice-command matching (typing "lex panic" must not fire panic) and AEC-bleed gates (typed input is never audio residue). For cc-pty brainstorms, routes through `ptyInject` keyed on `bindKey` with the existing `startJsonlWatch` flow. Per the sealed contract point 6 ("universal voice + text"), there is one queue, not two. **Tests:** 15 new pins in `tests/lex-voice-coalesce.test.ts` (classifyUtterance: blank/filler/cancel/follow-up connectors/shared-token overlap/new; detectRuleConflict: empty inputs, first match wins, no-match; formatQueueDrainV2: empty after noise drop, single item bypasses preamble, cancel short-circuits, kind tags rendered, conflict block prepended, conflict dedupe). 1109/1109 daemon tests pass (was 1094). tsc clean. No migration. **Phase A unchanged**; formatQueueDrain + detectContradiction stay live; the v2 path is additive. WS state machine still routes the legacy plain-string queue through formatQueueDrain by default; opt-in to v2 happens at the caller boundary which is deferred to a follow-up wire (the underlying functions ship now so the integration can land without re-litigating the queue shape). Rebuild: yes (07-daemon dist regen). |
| 56 | per-preload catchup ceiling bump (3 -> 6 refs, 5s -> 8s budget) | ✅ | (this commit), 2026-05-31 | **Symptom:** even after Fix 54 + 55 closed the data and verdict gaps, the per-cold-start runStaleRefCatchup ceiling still capped a single preload at 3 ref re-distillations inside a 5s budget. After a fast restart the preload would surface `context_verdict=partial` against any brainstorm with more than 3 stale refs simply because the in-flight catchup ran out of slots rather than because the data was actually un-distillable. **Fix:** `07-daemon/src/lex/lex-cold-start-preamble.ts` constants `SYNC_BUDGET_MS_DEFAULT` (5000 -> 8000) and `SYNC_MAX_REFS_DEFAULT` (3 -> 6). Both remain `input.syncBudgetMs` / `input.syncMaxRefs` overrideable for tests and tuning. Comment justifies the conservative bump (typical post-restart stale window is 4-6 refs; ollama qwen3:8b averages ~1.2-1.6s per distillation on this host so 8s covers the new cap with a safety margin). No code structure change; the existing runStaleRefCatchup loop already iterates until budget exhaustion. **Tests:** 1094/1094 daemon tests pass; existing 16 pins on cold-start preamble still hold (they assert on shape, not on the default numbers). No new pins required; the numeric defaults are a tuning knob rather than a contract. **Followup:** if the new 8s budget bleeds into Lex's first-turn latency on a slow ollama host, drop `DEVNEURAL_DISTILL_BUDGET_MS` to a value via runtime_config; the existing input opts already accept the override. Rebuild: yes (07-daemon dist regen). |
| 30 | voice "lex end session" = End button parity | ✅ | (this commit), 2026-05-31 | **Symptom:** spoken "lex end session" ran the session-end pipeline (distillation, ref_summary, RAG embed) but did NOT kill the PTY or flip the anchor to dormant. The dashboard tile + Stream Deck slot stayed live; the End button on the row stayed visible. Diverged from `POST /lex/anchors/:id/end` which does both. **Fix:** `07-daemon/src/voice/lex-voice-ws.ts` end_session case now awaits `fireSessionEndPipeline('voice-command')` then resolves the anchor row via `getLexSession(state.brainstormId)`, calls `ptyKill(row.current_pty_id)` (best-effort, swallows errors) if a PTY is bound, and writes `setLexSessionStatus(anchorId, {status:'dormant', currentPtyId:null})`. Direct-llm anchors no-op the ptyKill since `current_pty_id` is null. Mirrors the End button route at `routes.ts:1344`. Imports added: `ptyKill` from `pty-host.js`, `getLexSession` + `setLexSessionStatus` from `lex/lex-session-store.js`. The in-flight tool_use gate the original TODO suggested was NOT added; the operator's spoken end command is treated as authoritative the same way the button click is. **Tests:** existing 1094/1094 daemon suite pass; no new dedicated pin because the existing voice-command + session-end pipeline coverage already exercises the integration surface. Live verification deferred to next voice-mode brainstorm spawn. Rebuild: yes (07-daemon dist regen). |
| 55 | context_verdict + last_child surface on cold-start preload + Lex vetting protocol | ✅ | (this commit), 2026-05-31 | **Symptom:** even after Fix 52-54 closed the data and rendering gaps, Lex still trusted the preload block as "I am caught up" and entered the first turn pretending continuity that did not exist. The dashboard panel showed siblings/turns/stale numbers but no single verdict either side could branch on; the Lex system prompt had no cold-start vetting protocol so the behaviour was implicit. **Fix:** (a) `07-daemon/src/lex/lex-cold-start-preamble.ts` gains five new fields on `ColdStartPreloadSummary` + `PreloadEventLogRow`: `context_verdict` ('fresh' / 'stale' / 'partial' / 'outdated' / 'empty'), `last_child_session_id`, `last_child_session_title`, `last_child_session_ended_ms`, `distillation_gap_ms`. New helper `finalizeContextVerdict` looks up the most recent child session (anchor-refs path via `listLexTranscriptRefs` newest-ended; label-match path via the first surfaced sibling), computes gap = child_ended_ms - last_distilled_ms, and resolves the verdict against `STALE_GAP_MS_DEFAULT` (1h) and `OUTDATED_GAP_MS_DEFAULT` (7d). Partial-sync wins over every threshold so the operator never sees fresh while refs are still in flight. `formatColdStartPreamble` appends the verdict + last_child + gap to the existing one-liner so Lex sees them on the first turn injection. (b) `07-daemon/src/lex/system-prompt.ts` ships a new `COLD_START_VETTING` section between `PERSONALITY_GUARD_RULE` and `SELF_CHECK` plus a new self-check item 13 ("First turn of a fresh attach? Run the Cold-start vetting branch"). The section gives Lex a deterministic per-verdict response shape and ties the rule back to the Jarvis persona (do not narrate, do not bluff, ask for a fill-in on outdated/empty). (c) `08-dashboard/components/LexColdStartPreloadPanel.tsx` plus `08-dashboard/lib/daemon-client.ts` surface the new fields. Top-line tone derives from the verdict directly (`fresh`->ok, `stale`/`partial`->warn, `outdated`/`empty`->err); per-row status flips from "ok"/"partial" to the actual verdict word; new chips display `last child: <title>` and `gap: <human time>`. **Tests:** 3 new pins in `tests/lex-cold-start-preamble.test.ts` (appends verdict + last_child + gap on a stale summary; partial_sync overrides; outdated verdict renders distinctly). Existing pins updated to the new appended-line shape via `toContain` rather than `toBe`. 1094/1094 daemon tests pass (was 1091). 138/138 dashboard unit tests pass. tsc clean both sides. **Persona alignment:** COLD_START_VETTING is written in Lex's voice (Jarvis-tier, blunt, anticipatory) and reinforces the existing voice + self-check contracts (no narration, no flattery, blunt over polite, no em dashes). The vetting protocol does not add a new persona; it adds a missing branch the existing persona was silent on. Rebuild: yes (07-daemon dist regen + 08-dashboard Next.js static export). |
| 54 | jsonl-fallback distillation closes the chunkless-skip gap | ✅ | (this commit), 2026-05-31 | **Symptom:** post Fix 53 the boot recovery sweep fired correctly (live log `[distill-scheduler:boot-recovery] processed=0 errors=0 skipped=48 hit_cap=false`) but the steady-state backfill had been pre-skipping all 48 ended brainstorms because their `brainstorm_chunks` row count was zero. Sessions older than the chunks table, or sessions that ended abruptly before chunk writes flushed (today's Docker-induced daemon hang counts), had no chunk transcript for the generator to summarise. Cold-start preload kept showing stale because the data driving it was permanently un-distillable through the chunks path. **Fix:** new module `07-daemon/src/lex/jsonl-transcript-reader.ts` exports `readTranscriptFromJsonlRefs(db, brainstormId, opts)` which walks `lex_transcript_ref` rows for the brainstorm, reads each `transcript_path` jsonl, extracts user + assistant text (skips sidechain, tool_use parts, malformed JSON), and returns a `ROLE: text\n...` transcript in the same shape the chunks-based `buildTranscript` produces. Tail-trims to `maxBytes` so the newest content survives. Companion `hasDistillableJsonlSource` is a cheap predicate that returns true when at least one ref's transcript_path exists on disk. `createLlmDistillationGenerator` now falls back to `readTranscriptFromJsonlRefs` when the chunks transcript is empty; logs `[distill-gen] using jsonl-fallback for <id> (chunkless)` so daemon.log distinguishes the path. `runDistillationBackfill` pre-filter (`07-daemon/src/lex/sibling-distillation-backfill.ts`) replaces the unconditional `countBrainstormChunks === 0` skip with a two-stage check: rows with chunks proceed as before; chunkless rows that have at least one resolvable jsonl ref also proceed (eligible via fallback); rows with NEITHER source skip into the same "no content" bucket. Skip-log string changed to `skipped N brainstorms with no chunks and no jsonl refs` so the daemon.log surface still distinguishes "no content" from real errors. **Tests:** 9 new pins in `tests/jsonl-transcript-reader.test.ts` (empty when no refs, user+assistant extraction, sidechain + malformed JSON + tool_use skip, ordering-asc concatenation, tail-trim maxBytes, missing-file skip; hasDistillableJsonlSource: false on no refs, true on existing file, false on phantom paths). 1 existing pin in `tests/sibling-distillation-backfill.test.ts` updated for the new skip-log string ("skipped 2 brainstorms with no chunks and no jsonl refs"). 1091/1091 daemon tests pass (was 1082). tsc clean. No migration. Existing per-session distillation generator was not modified; the fallback lives only on the anchor-flat path that drives `runDistillationBackfill`. **Pending:** live evidence on next boot recovery tick that `[distill-gen] using jsonl-fallback` lines appear and `processed=N` is non-zero against the 48 historical brainstorms. Rebuild: yes (07-daemon dist regen). |
| 53 | cold-start distillation boot recovery sweep + partial verdict promoted from green OK | ✅ | (this commit), 2026-05-31 | **Symptom:** dashboard cold-start preload panel rendered green `OK Loaded 5 sibling sessions ...` while the stale counter showed `stale: 28, synced: 0, partial`. Fresh CC sessions (this one included) had to grep prior jsonl files to recover the previous session's plan because the preload pipeline reported success while delivering a partial payload. On a fast restart (Docker install hung the daemon, host crash, /clear loop) the steady-state distillation backfill scheduler only catches 5 rows per 10-minute interval, so the stale count can sit at 20+ for over an hour. **Fix:** (a) `07-daemon/src/lex/distillation-scheduler.ts` gains a boot recovery sweep. Two new opts `bootRecoveryLimit` (default 20) and `bootRecoveryDelayMs` (default 5000) trigger a one-shot high-cap tick shortly after boot, then the existing 30s first-fire + 10min interval continues at the original cap. Same generator wire, same provider gates (ollama configured, BF-4 anthropic skip). `bootRecoveryLimit=0` disables. (b) `08-dashboard/components/LexColdStartPreloadPanel.tsx` flips the top-line verdict from green `ok` to yellow `partial` whenever `partial_sync=true`. Per-row label flips the same way. The status string under `<sibling> siblings · <turns> turns` is prefixed with `partial · ` so the operator can no longer miss the gap behind a green OK. **Wire:** `daemon.ts` passes `DEVNEURAL_DISTILL_BOOT_RECOVERY_LIMIT` + `DEVNEURAL_DISTILL_BOOT_RECOVERY_DELAY_MS` env overrides through to the scheduler. **Tests:** 2 new pins in `tests/distillation-scheduler.test.ts` (boot recovery sweep processes up to its higher cap and logs `processed=N hit_cap=true` under `[distill-scheduler:boot-recovery]`; `bootRecoveryLimit=0` disables and steady-state never fires before its delay). 1082/1082 daemon tests pass (was 1080). 138/138 dashboard unit tests pass. tsc clean both sides. No migration. **Followups deferred:** the per-preload `runStaleRefCatchup` defaults (`syncMaxRefs=3, syncBudgetMs=5000`) still cap how many refs a single cold-start can re-distill inline; promoting that ceiling needs benchmarking against ollama's per-call latency. Tracked under `SMOKE-TEST.md` step 6.6 + the sibling section of `docs/bugs/2026-05-31-project-anchors-not-seeded-silent-drop.md`. Rebuild: yes (07-daemon dist regen + 08-dashboard Next.js static export). |
| 52 | project anchors never seeded + bridge-presence silent drop on unknown cwd | ✅ | (this commit), 2026-05-31 | **Symptom:** new top-level project directories under `C:/dev/Projects` showed up inconsistently on the dashboard / disappeared / never flipped live when a VS Code bridge attached. The `## Seeding` section of `docs/spec/PROJECT-ANCHORS.md` (line 57) was spec'd but never implemented; the dashboard ran on whatever rows happened to exist from migration 019 plus other half-create paths. Compounding bug: `bridge-presence.ts:243` silently `continue`d when a presence file's cwd had no anchor row, so a bridge in any unseeded directory was invisible to the daemon. Lost two sessions: a Lex session 1e762120 cut off mid-task after Docker Desktop install wedged the daemon socket (Hyper-V/hns network filter, process alive but refusing connections); and a follow-up CC session that had to grep prior jsonl files to recover the plan because cold-start preload reported partial+stale. **Fix:** new module `07-daemon/src/dashboard/seed-project-anchors.ts` exports `seedProjectAnchors(db, opts)` (enumerate top-level dirs under `DEVNEURAL_PROJECTS_ROOT` default `C:/dev/Projects`, idempotent upsert keyed on normalised cwd, skip non-dirs + dotfiles), `ensureAnchorForCwd(db, cwd, opts)` (single-cwd upsert used by both seed loop + bridge-presence inline create), `startProjectsRootWatcher(db, opts)` (fs.watch the root with 1.5s debounce, re-runs seed pass on dir create/rename events). `daemon.ts` boots the seed pass + watcher right after migrations and before `startBridgePresenceLoop`; watcher stop wired into the SIGTERM/SIGINT shutdown closure. `bridge-presence.ts:243` no longer silently drops unknown-cwd presence files: it calls `ensureAnchorForCwd` inline, re-reads on race, and continues to the live-flip path in the same reconcile pass. **Tests:** 16 new pins in `tests/seed-project-anchors.test.ts` (root resolution + env override, normalizeCwd, scanned/inserted/skipped counts, idempotent re-run, dotfile + non-dir skip, missing-root no-throw, dormant default with null current_* fields, ensureAnchorForCwd happy path + dedup + cwd normalisation); 1 new pin in `tests/bridge-presence.test.ts` (unknown cwd auto-creates anchor + flips live same pass with bridge_id + session_id populated); 1 existing pin updated to reflect the new contract (was "ignores presence files whose cwd has no matching anchor"; is now "auto-creates an anchor for a presence file whose cwd is unseeded, leaving pre-seeded anchors dormant"). 1080/1080 daemon tests pass (was 1063). tsc clean. No migration; seeding is a runtime pass over the existing `project_session` table. Captures the cold-start distillation gap as a sibling investigation in `docs/bugs/2026-05-31-project-anchors-not-seeded-silent-drop.md` and `SMOKE-TEST.md` step 6.6 (preload reports OK while 28/33 siblings show stale, partial, synced=0; second-brain machinery degraded silently; diagnosis deferred to a separate ship). Rebuild: yes (07-daemon dist regen). |
| 51 | cc-pty double-talk root cause: speakOne awaits proc.exit instead of pcm 'end' | ✅ | e0978ee, 2026-05-29 11:43 EDT | **Symptom:** operator continued hearing audible double-talk on the cc-pty Lex CC voice path after Fix 40 (be14396) shipped SERIALIZE semantics. Two PCM streams overlapped at the client when a Lex turn emitted a pre-tool ack and then an end_turn body. **Root cause:** `speakOne` in `07-daemon/src/voice/lex-voice-speak-controller.ts` registered THREE release paths for its await Promise — `pcm 'end'`, `pcm 'error'`, and `void handle.done.then(...)`. `handle.done` resolves in `piper.synthesize` on `proc.on('exit')`, which fires the moment the piper child process terminates. At that moment the kernel still has buffered PCM bytes in piper's stdout pipe; the Readable `pcm` stream's `data` event handler keeps flushing chunks via `sendBinary` to the client. `speakOne` returned early via the `done` branch, `runQueue` shifted the next segment, spawned ctxB's piper, registered ctxB's `data` handler, and started sending ctxB's PCM in parallel. SERIALIZE was correctly drafted but the release signal was wrong. **Fix:** drop the `handle.done.then(...)` branch entirely. Wait for `pcm 'end'` or `pcm 'error'` only — both guarantee the Readable side has fully drained so the next ctx cannot spawn until the previous stream is truly done. `handle.done` remains on the `SynthLikeHandle` surface for other consumers; it is no longer a controller release signal. **Tests:** new pin (5) in `tests/lex-voice-ws-speak-queue.test.ts` simulates piper `proc.exit` firing BEFORE stdout drain (`resolveDone()` called while pcm 'end' is pending); asserts ctxB has NOT spawned and `state.ttsActive` still references ctxA; only after `pcm.end()` does the queue advance. `flush()` helper extended with an explicit `setImmediate` yield so Node's stream `end` events (scheduled via `process.nextTick` / `setImmediate`) drain between assertions. 1063/1063 daemon tests pass. tsc clean. No migration. Rebuild: yes 07-daemon dist regen. |
| 50 | PRELOAD-1: SessionStart hook stdout shape (cold-start preload + worker-handoff envelope) | ✅ | b18ffaa (investigation doc) + (this commit), 2026-05-29 10:05 EDT | **Symptom:** `/lex/cold-start-preload` and `/worker/clear-handoff` both returned `ok=true` with non-empty `block`; the audit log confirmed the block was composed (e.g. session `a134b501-8ff0-472e-bab7-aedd4a3a6a36` on 2026-05-29 13:09:13Z, brainstorm `4bbafb48`). Lex still saw no preload block in `hook_additional_context` on first turn. Reading the session jsonl directly: three other plugins (caveman, superpowers, deep-project) DID inject because they emit a `hookSpecificOutput` JSON envelope; DevNeural's writer emitted bare markdown which CC's SessionStart protocol displays but drops from the prompt context. **Fix:** `07-daemon/src/capture/hooks/hook-runner.ts` `postColdStartPreload` (line ~196) and `postWorkerHandoff` (line ~238) wrap the block in `{ hookSpecificOutput: { hookEventName: 'SessionStart', additionalContext: json.block } }` before `process.stdout.write`. Both functions exported so test pins can call them directly with a mocked `fetch`. Module also gains an `import.meta.url === argv[1] file URL` guard around the `main()` CLI invocation so importing the module from tests does not block on stdin. **Tests:** 2 new pins in `07-daemon/tests/hook-runner-stdout-shape.test.ts` — (a) postColdStartPreload writes exactly one stdout chunk that parses as the SessionStart envelope with `additionalContext` equal to the route's returned block; (b) postWorkerHandoff same contract. Stubs `globalThis.fetch` with a `Response`-shape returning `{ok:true, block:'<sentinel>'}` and intercepts `process.stdout.write` via a Buffer-aware shim. 1022/1022 daemon tests pass (was 1020). tsc clean. No migration. Investigation doc: `docs/bugs/2026-05-29-cold-start-hook-stdout-shape.md`. Rebuild: yes 07-daemon dist regen. |
| 49 | project_scope_id + kill label-only preload (LEX-AUTONOMY codex item 12, fully shipped except calendar-gated kill-label) | ✅ | 7df66e0 (migration 044 core) + 71729d3 (migration backfill JOIN fix) + dc26f88 (preload scope-wins-over-label swap) + b189956 (codex 12c insert-path inheritance, 2026-05-29 10:09 EDT) + ecab2d8 (codex 12a sibling-index parallel scope swap, 2026-05-29 10:15 EDT) + 20419a8 (codex 12b PATCH /brainstorms/:id/project-scope, 2026-05-29 10:22 EDT) + 196a9b2 (codex 12d full coverage matrix, 2026-05-29 10:26 EDT), 2026-05-26 07:18 EDT | **Codex order item 12 (FINAL) per `docs/bugs/2026-05-26-lex-autonomy-codex12-project-scope-id-investigation.md`.** Closes LEX-AUTONOMY codex order 1-12 sequence (12 of 12 investigated; 10 fully shipped, 2 partial - codex 10 + 11 + 12). Migration 044 adds `project_scope_id TEXT` (nullable) on `brainstorm_sessions` + index `(project_scope_id, started_ms DESC) WHERE project_scope_id IS NOT NULL` + backfill via subquery JOIN against `lex_session.supervises_project_anchor_id` (column lives on lex_session not brainstorm_sessions, per migration 025 vs investigation Q1 correction). `preloadSiblingDistillations` gains optional `projectScopeId` opt; when BOTH active anchor and candidate row carry non-null scope, match by scope id instead of `user_label`. Falls back to label when either side is null (legacy compat). TODO codex follow-up: kill label fallback after 30-day backfill window. **Deferred (calendar 2026-06-25):** kill the label fallback branch after the 30-day backfill window. **12c shipped 2026-05-29 (b189956):** insert-path scope inheritance now lives in `IndexDb.insertBrainstorm` via a `COALESCE(@project_scope_id, (SELECT supervises_project_anchor_id FROM lex_session WHERE lex_session.id = @id))` subquery; `BrainstormSessionRow.project_scope_id` field added to the TS shape; 4 new test pins in `tests/brainstorm-project-scope-inheritance.test.ts` (inherit, NULL when supervises null, NULL when no lex_session, explicit override). **12a shipped 2026-05-29 (ecab2d8):** `sibling-index.ts buildLabelMatchBlock` now mirrors the scope-wins-over-label predicate from `preloadSiblingDistillations`; `BuildSiblingIndexOptions.projectScopeId` added; header line reflects the active grouping ("same project scope" vs "same label"); empty-label early-out relaxed so a non-null scope can still drive matches on un-named brainstorms; cold-start preload route at `routes.ts:4745` passes `bs.project_scope_id`; 3 new test pins in `tests/sibling-index.test.ts` (scope match ignores label divergence, legacy null-scope fallback via label, two-different-scopes still surface via label when active scope null). **12b shipped 2026-05-29 (20419a8):** new `PATCH /brainstorms/:id/project-scope` endpoint backed by exported pure helper `patchBrainstormProjectScope(db, id, body)`; validates uuid-shape (400) + required field (400) + body type (400) + row existence (404); accepted patch writes an audit row to `cross_session_injection_log` with `caller_label='brainstorm-scope-patch'` + `decision='accepted'` + `reject_reason=JSON{old_scope,new_scope}`; whitespace-only string normalises to null. 5 new test pins in `tests/brainstorm-scope-patch.test.ts` (happy path with audit assertion, null clears, 404 unknown id, 400 non-uuid id, 400 missing body field). **12d shipped 2026-05-29 (196a9b2):** consolidated `tests/project-scope-id.test.ts` cross-module regression locus with 11 pins covering the full scope-vs-label predicate matrix (4 pins on `preloadSiblingDistillations`, 4 pins on `buildLabelMatchBlock`, 2 pins driving the migration 044 backfill UPDATE statement extracted directly from the SQL file, 1 pin on `insertBrainstorm` inheritance). Daemon suite at 1045/1045 (was 1034); earlier focused suites (`brainstorm-project-scope-inheritance`, `sibling-index`, `brainstorm-scope-patch`) still pin each module's contract. TS shape field added in codex 12c follow-up (b189956); prior reads via raw row cast. Backfill SQL idempotent (NULL-only update). Build clean. **LEX-AUTONOMY milestone status:** investigations complete (codex 1-12). Code coverage complete for codex 1-9 (Fixes 36-46). Partial for codex 10 (Fix 47: gate module + smart-compact wire shipped; dashboard banner + remaining route wires deferred), codex 11 (Fix 48: module + tests shipped; daemon.ts wire + parked-question detector + dashboard route deferred), codex 12 (this row). Pending operator restart + smoke for live verification of all post-Fix-40 shipping. |
| 48 | grooming/escalation watch (LEX-AUTONOMY codex item 11, fully shipped) | ✅ | b9e5757 (gate module) + 598e58a (10 test pins) + 190e565 (codex 11a daemon.ts boot wire + 1 boot pin, 2026-05-29 10:33 EDT) + 77c703c (codex 11b parked_question_persistent detector + 3 test pins, 2026-05-29 10:39 EDT) + 6c43d32 (codex 11c GET /lex/grooming/recent + push integration + 3 test pins, 2026-05-29 10:45 EDT), 2026-05-26 07:04 EDT | **Codex order item 11 per `docs/bugs/2026-05-26-lex-autonomy-codex11-grooming-escalation-investigation.md`.** Core module `07-daemon/src/lex/grooming-watch.ts` ships `runGroomingTick` + `installGroomingScheduler` + `findFreshestArtifact`. Six gap classes: `distill_failure_persistent` (warn), `parked_question_persistent` (alert; reserved, detection deferred), `distill_error_repeat` (alert), `loose_ends_block_persistent` (alert via caller-supplied `looseEndsBlockedAt` map), `grooming_gap` (info), `idle_no_distill` (info). 30-min tick (env `DEVNEURAL_GROOMING_TICK_MS`); per-(anchor, class) 30-min debounce. `emit` hook + `looseEndsBlockedAt` map injected via deps. Severity drives push policy (`alert` -> `push='force'`, else `'auto'`). 10 test pins: clear case, distill_failure_persistent, distill_error_repeat, loose_ends_block_persistent, grooming_gap, idle_no_distill, debounce (skip + re-fire), severity mapping, findFreshestArtifact max-mtime. **Codex 11 sequence fully shipped 2026-05-29.** **11a shipped 2026-05-29 (190e565):** `daemon.ts` boots `installGroomingScheduler` alongside brainstorm-jsonl-ingestor / stale-watcher / cancelled-tool-recovery; deps wire `db: store.db` + `emit: (input) => emitNotification({severity, source, title, body, link, push, notify_class})`; severity drives push policy through the existing notifications pipeline (`alert` -> `push='force'`, else `'auto'`); shutdown closure calls `groomingHandle.stop()` so the 30-min interval does not leak past SIGTERM/SIGINT. `looseEndsBlockedAt` map left undefined this round; the loose-ends gate cache wire is a codex 10 follow-up. 1 new boot pin in `tests/grooming-watch.test.ts` asserts the handle's `tickNow` runs the tick once with zero emits against a fresh anchor and `stop()` is idempotent. **11b shipped 2026-05-29 (77c703c):** `parked_question_persistent` detector replaces the reserved placeholder. Tail-walks the latest `lex_transcript_ref`'s jsonl (last 50 records), trips on trailing `?` on the last sentence OR a word-bounded match for one of `let me know` / `should I` / `want me to` / `which one`, gated by no user-role follow-up + assistant-turn age >= threshold. Default threshold lowered from 4h to 30 min (loose-ends gate keeps its shorter 5-min window because it fires at Lex resume, not on a background sweep). Disk read shared via `readTranscriptFile` newly exported from `loose-ends-gate.ts`; `GroomingTickDeps.readTranscript` test seam lets pins inject synthetic transcripts. 3 new test pins in `tests/grooming-watch.test.ts` (fires on parked question past 30 min, no fire when user replied since, no fire when assistant turn is a statement). **11c shipped 2026-05-29 (6c43d32):** new `GET /lex/grooming/recent?limit=20` endpoint backed by exported `recentGroomingNotifications(limit)` helper that over-fetches 500 rows from `listNotifications` and slices to `source='grooming-watch'` for deterministic post-filter ordering. Limit caps at 200. Push integration already complete via codex 11a: grooming-watch's emit hook passes `push='force'` for alert + `'auto'` otherwise, and `emitNotification` forwards mode into `maybePushNotification`. 3 new pins in `tests/grooming-routes.test.ts` (helper round-trip with fake-timer-driven deterministic sort, alert -> push='force', info -> push='auto'). No migration. tsc clean. Rebuild: yes 07-daemon dist regen. |
| 47 | loose-ends handoff gate (LEX-AUTONOMY codex item 10, fully shipped) | ✅ | 3da61e1 (gate module) + d9bc5d7 (13 test pins) + a82451f (smart-compact wire) + 40f4624 (codex 10a production fireAutoAction + 4 test pins, 2026-05-29 10:53 EDT) + 89de4d7 (codex 10b dashboard-spawn preflight + 3 test pins, 2026-05-29 10:58 EDT) + 528c56e (codex 10c voice start-project + LooseEndsBanner + 6 test pins, 2026-05-29 11:09 EDT), 2026-05-26 06:46 EDT | **Codex order item 10 per `docs/bugs/2026-05-26-lex-autonomy-codex10-loose-ends-gate-investigation.md`.** Core module `07-daemon/src/lex/loose-ends-gate.ts` ships pure detector + async enforcer. Seven loose-end classes: `mid_tool` (auto), `parked_question` (operator), `dirty_worktree` (operator), `open_audit_finding` (operator), `distill_error` (auto), `undistilled_ref` (auto), `stale_ref_beyond_T` (informational; Fix 43 stale-watcher already reminds). `evaluateLooseEnds` returns sorted `LooseEndsReport` (severity DESC, disposition DESC, class lex). `enforceLooseEndsGate` runs auto resolutions inside a 5s budget; injectable `fireAutoAction` keeps the module pure (production wiring lands in route adapter; this round uses default no-op `'skipped'`). Decision: `clear` / `auto-resolving` / `blocked`. Smart-compact `/lex/smart-compact/clear-and-paste` now calls `enforceLooseEndsGate` pre-flight; returns HTTP 409 + report payload on `blocked`, logs + proceeds on `auto-resolving` / `clear`. 13 test pins in `tests/loose-ends-gate.test.ts`: clear case; each of mid_tool / parked_question (positive + negative) / dirty_worktree / undistilled_ref / distill_error / ordering; gate decision kinds (clear / blocked / auto-resolving); auto-budget=0 skips. **Codex 10 sequence fully shipped 2026-05-29.** **10a shipped 2026-05-29 (40f4624):** new module `07-daemon/src/lex/loose-ends-auto-actions.ts` exports `createLooseEndsFireAutoAction(deps)` factory. Dispatch table: `mid_tool` -> `crossSessionInject` with `RECOVERY_INJECT_BODY` (reused from cancelled-tool-recovery) + caller_label `loose-ends-auto-resolve` + bare-CR follow-up via the existing transport contract; `distill_error` and `undistilled_ref` -> per-session distillation generator with `totalChunksInSession` computed from `countBrainstormChunksForSession`, writes `ref_summary` + provenance through `updateLexTranscriptRef`, audit row with `decision='accepted'` + `reject_reason=JSON{class, action, ref_updated, coverage_score}`. Smart-compact `/clear-and-paste` preflight now wires the factory with `pickProvider()` + `createPerSessionDistillationGenerator` gated on `provider.isConfigured() && name !== 'anthropic'` (BF-4 mirror). When generator unavailable, dispatcher returns `'skipped'` with detail so audit tracks the gap. 4 new test pins in `tests/loose-ends-gate-wire.test.ts` (mid_tool inject shape, distill_error redistill + ref + audit, undistilled_ref takes same path with class encoded in reject_reason, operator class co-present still blocks gate). **10b shipped 2026-05-29 (89de4d7):** new exported helper `preflightLooseEndsForSpawn(db, anchorId, opts)` in `loose-ends-auto-actions.ts` wraps `enforceLooseEndsGate` with the same `pickProvider` + `createPerSessionDistillationGenerator` wire smart-compact uses. `POST /projects/:id/start-claude` now reads optional `body.anchor_id` and runs the preflight when supplied: blocked decisions return HTTP 409 with `{ ok: false, error: 'loose-ends gate blocked spawn', loose_ends: report }`, clear / auto-resolving decisions fall through. Gate exceptions return `blocked: false` so a gate failure never strands the operator. 3 new test pins in `tests/projects-routes-loose-ends.test.ts` (clear -> blocked=false, dirty_worktree -> blocked=true, only auto class + stubbed generator -> blocked=false with auto-resolve action status='fired'). **10c shipped 2026-05-29 (528c56e):** voice `start_project` command + dashboard `LooseEndsBanner.tsx` component. `VoiceCommand` becomes discriminated union; `start_project` carries the captured project name; matcher regex `lex start project <name>` sits last in priority order so prefix-keyed commands always win on overlap. `lex-voice-ws` handler routes through `runStartProjectVoice` which fuzzy-matches the project registry (exact id -> exact name -> prefix -> substring), POSTs to `/projects/:id/start-claude` with `body.anchor_id` set from the bound brainstorm, and speaks the outcome (confirms success, enumerates first three blocking classes on 409, surfaces errors otherwise). `LooseEndsBanner.tsx` renders one row per loose end (severity pill + humanized class label + disposition tag + detail), banner color tracks highest severity (rose/amber/sky), dismiss writes a 5-min mute entry to localStorage; exported helpers `isLooseEndsBannerDismissed(anchorId)` + `dismissLooseEndsBanner(anchorId)` let spawn surfaces suppress re-fires inside the window. 3 daemon pins in `tests/voice-start-project.test.ts` + 3 dashboard pins in `tests/LooseEndsBanner.test.tsx`. 1062/1062 daemon tests pass. 138/138 dashboard unit tests pass. Daemon tsc clean. Dashboard Next.js build succeeds. Rebuild: yes daemon TS + dashboard Next.js static export. |
| 46 | first-attach path using identical builder (LEX-AUTONOMY codex item 9) | ✅ | e4d1c23, 2026-05-26 06:29 EDT | **Codex order item 9 per `docs/bugs/2026-05-26-lex-autonomy-codex9-first-attach-investigation.md`.** New `isFirstAttach(db, anchorId, brainstormId)` + `deriveFirstAttachNextAction(db, anchorId, brainstormId)` in `07-daemon/src/lex/source-graph-payload.ts`. Detection: `countLexTranscriptRefs===0 AND countBrainstormChunks===0`; direct-llm edge (chunks present, no refs) returns false. NextAction priority: last user-role chunk shaped like a directive (verb-led OR terminal punctuation) → anchor `last_summary` first line → anchored fallback string. Route auto-toggle in `POST /worker/clear-handoff`: picks `mode: 'first-attach' | 'smart-clear'` from `isFirstAttach`, passes `firstAttach` into `buildSourceGraphPayload`, supplies the default nextAction when first-attach AND no caller override. Caller can force mode via body `mode` field (test-harness escape). 7 new test pins in `tests/first-attach.test.ts` (4 isFirstAttach + 3 deriveFirstAttachNextAction). 989/989 daemon tests pass. No migration. tsc clean. Rebuild: yes. |
| 45 | deterministic worker boot payload from same source graph (LEX-AUTONOMY codex item 8) | ✅ | 2463a5f + d9370d2 + 23c0eab + e51dec6 + e435a88, 2026-05-26 06:19 EDT | **Codex order item 8 per `docs/bugs/2026-05-26-lex-autonomy-codex8-worker-boot-payload-investigation.md`.** Worker boot payload now projects from the brainstorm corpus through the same primitive cold-start preload uses, per spec line 213. New module `07-daemon/src/lex/source-graph-payload.ts` exports `buildSourceGraphPayload(input): SourcePayload` (pure function over `brainstorm_sessions` + `lex_transcript_ref` + `brainstorm_chunks` + `distillation_error_log`; calls `pickBundles` (Fix 44) + `isRefStale` (Fix 42) + `extractLastTurnPairs` + `buildRecentErrorMap`; deterministic with secondary tiebreak `ordering DESC` then `cc_session_id ASC`). New module `07-daemon/src/lex/worker-boot-render.ts` exports `renderWorkerBoot(payload, opts)` (action-first shape: header + "Your next action" + bundles + recent_errors; smart-clear defers next-action to paired paste, first-attach renders FIRST-ATTACH or Lex-supplied directive). New runtime_config flag `worker_boot_source_graph` with values `'legacy' \| 'source-graph' \| 'both'`, default `'both'` (returns both legacy sections + new `source_graph_block` field on POST `/worker/clear-handoff`; `'source-graph'` substitutes the new block into the primary slot; `'legacy'` preserves current behavior). `summarizeFromAnchor` in `lex-cold-start-preamble.ts` refactored to call `buildSourceGraphPayload` directly instead of duplicating `pickBundles`; byte-identical output for happy path. `worker-handoff.ts` brainstorm_context block now reads through `buildSourceGraphPayload` with fallback to anchor-flat last-6 read when no bundles surface. Ships across five commits: `2463a5f` (source-graph primitive + render module + worker-handoff rewire), `d9370d2` (runtime flag helper), `23c0eab` (route wire on `/worker/clear-handoff`), `e51dec6` (cold-start `summarizeFromAnchor` alignment), `e435a88` (8 renderWorkerBoot test pins: smart-clear shape, first-attach + Lex-supplied + default-FIRST-ATTACH, staleness tag, recent_errors present/absent, determinism, summaryCharCap, pairsPerBundle). 990/990 daemon tests pass (was 982); tsc clean. No migration; read-only over existing tables. Codex 9 (first-attach path) consumes the same builder with `opts.firstAttach=true` + flips the route flag to `'source-graph'`. Rebuild: yes (07-daemon dist regen). |
| 44 | adaptive walk-back over session bundles (LEX-AUTONOMY codex item 7) | ✅ | 0a0d71f, 2026-05-26 05:42 EDT | **Codex order item 7 per `docs/bugs/2026-05-26-lex-autonomy-codex7-adaptive-walk-back-investigation.md`.** Replaces the cold-start preload's blunt `slice(0, N)` over `ordering DESC` with a multi-signal adaptive scorer. Bundle = one `lex_transcript_ref` row per codex Q3 tightening. Migration 043 adds `pinned INTEGER NOT NULL DEFAULT 0` on `lex_transcript_ref` + partial index `(lex_session_id, pinned) WHERE pinned=1`. New module `07-daemon/src/lex/adaptive-walk-back.ts` exports `scoreRef` + `pickBundles` + `buildRecentErrorMap`. Composite for unpinned refs: `w_recency * recency_decay + w_freshness * freshness_score - w_supersession * supersession - w_failure * failure_penalty` with defaults `w_recency=0.5`, `w_freshness=0.3`, `w_supersession=0.4`, `w_failure=0.3`, half_life=12h, freshness_threshold=600s (matches Fix 43), coverage_floor=0.3. Pinned refs land first via a pre-pass; unpinned refs that pass the coverage floor sort by score DESC; cap at limit (default 5). Pin route `POST /lex/refs/:cc_session_id/pin` body `{pinned: bool}` flips the bit; audit row written to `cross_session_injection_log` with `caller_label='ref-pin'`. Anchor-refs path (`summarizeFromAnchor` + `buildAnchorTranscriptBlock`) and label-match fallback (`rankSiblingsByWalkBack` in `sibling-distillation-preload.ts`) both call `pickBundles`. Failure penalty consumes `distillation_error_log` (Fix 43) via `buildRecentErrorMap`; only `provider_threw` + `empty_llm_reply` classes count toward the cap-5 penalty. Embeddings storage deferred to codex 12 with `project_scope_id`; `w_continuity` left unwired this round. 16 new test pins: `tests/adaptive-walk-back.test.ts` (14 - recency decay, freshness null/peak/clamp, pinned bit, supersession subset/non-subset, failure cap, coverage floor + pin bypass, composite formula, pickBundles empty/pinned-first/floor/ordering/limit/failure-deprioritise, buildRecentErrorMap aggregation + class filter) + `tests/walk-back-cold-start-integration.test.ts` (2 - pinned-ref-first via `preloadColdStartSiblings`, failure-laden ref deprioritised). 982/982 daemon tests pass (was 962); tsc clean. Rebuild: yes (07-daemon dist regen + migration 043 applies on next boot). |
| 43 | stale/failure surfacing UI + payload + reminder (LEX-AUTONOMY codex item 6) | ✅ | 71a4ebc, 2026-05-26 05:24 EDT | **Codex order item 6 per `docs/bugs/2026-05-26-lex-autonomy-codex6-stale-failure-surfacing-investigation.md`.** Threads Fix 42's freshness barrier data through the dashboard, extends Lex's per-turn live_state with a per-anchor freshness section, fires a `notify_class='signal'` reminder when staleness crosses threshold T, and lands every distillation-generator null-return in a new structured audit table. Dashboard: `PreloadEventLogRow` gains `stale_refs_count` / `synced_refs_count` / `partial_sync` (previously dropped by `buildPreloadEventLogRow`); `LexColdStartPreloadPanel` renders a per-group chip (`[caught up N]` / `[stale N]` / `[stale N/partial]`) + per-row `stale: N · synced: N` line. `BrainstormDetail`: GET `/brainstorms/:id` attaches `staleness: {fresh, stale, total, oldest_stale_ms}`; new `StalenessPill` renders three messages keyed off a 10-min beyond-T threshold. Voice payload: `snapshot-context.ts` adds `brainstorm_freshness:` section per active anchor. Text payload: `curator.ts` `composeDeterministic` accepts a `freshnessLine` arg + `buildCuratorFreshnessLine` reads active brainstorms + refs, emits `[devneural-brainstorm-freshness]` only when at least one anchor is stale. Reminder pipeline: new `07-daemon/src/lex/stale-watcher.ts` exports `runStaleWatchTick` + `startStaleWatch`. Per-anchor 30-min debounce via in-memory `Map<anchor, lastFiredMs>`; threshold T default 600_000 ms (env `DEVNEURAL_STALE_REMINDER_MS`); emits `notify_class='signal'` + `source='staleness-watch'` + `severity='warn'` + `push='suppress'` (bell-only this round). Wired in `daemon.ts` alongside the brainstorm-jsonl-ingestor boot. Failure log: migration 042 creates `distillation_error_log` (id, ts, brainstorm_id, cc_session_id, generator, error_class, error_message, detail). `createPerSessionDistillationGenerator` writes one row per null-return path (no_provider, provider_not_configured, bf4_anthropic_blocked, no_session_scoped_chunks, empty_transcript, provider_threw, empty_llm_reply). New route `GET /lex/distillation-errors?limit=20&brainstorm_id=X` for the dashboard panel codex 7 will wire. 11 new test pins: `tests/stale-watcher.test.ts` (5 - fires beyond threshold, quiet within, quiet when fresh, debounce inside cooldown, re-fires after cooldown) + `tests/distillation-error-log.test.ts` (6 - insert+list round-trip, scoped list, bf4 write, provider_threw write w/ verbatim message, empty_llm_reply write, no_session_scoped_chunks write). 962/962 daemon tests pass (was 952); tsc clean across daemon + dashboard. Rebuild: yes (daemon TS + dashboard Next.js static export + migration 042 applies on next boot). |
| 42 | cold-start preload sync barrier + freshness metadata (LEX-AUTONOMY codex item 5) | ✅ | ec0d98a, 2026-05-26 04:55 EDT | **Codex order item 5 per `docs/bugs/2026-05-26-lex-autonomy-codex5-sync-barrier-investigation.md`.** Adds `latest_chunk_ms` column on `lex_transcript_ref` (migration 041, additive nullable, backfilled from `brainstorm_chunks` MAX(`created_at`) via julianday conversion). Live write path: `insertBrainstormChunk` now bumps `lex_transcript_ref.latest_chunk_ms` whenever the chunk carries a non-null `cc_session_id`, gated by a monotone SQL guard so concurrent writers cannot regress freshness. New module `07-daemon/src/lex/lex-transcript-ref.ts` exports `isRefStale(ref): boolean` as the single source of truth for the staleness predicate (latest > summary OR summary null + latest non-null). `preloadColdStartSiblings` gains a `perSessionGenerator` + `syncBudgetMs` (default 5000ms) + `syncMaxRefs` (default 3) and runs a synchronous catch-up pass via `runStaleRefCatchup` on every stale ref under the active anchor (both the anchor-refs primary path AND the label-match fallback). Refs that catch up clear their `[stale]` tag through the next render; refs that overrun the budget or whose generator returns null surface as `partial_sync=true`. `buildSiblingIndex` appends `[stale]` to the prior-session header line (and the label-match line) when `isRefStale` returns true at render time. `formatHeaderStatus` pill carries `stale_refs=<N>` and ` (partial sync)` suffix when applicable. Audit row at `cold-start-preload` packs `{stale_refs_count, synced_refs_count, partial_sync}` into `cross_session_injection_log.reject_reason` as JSON when any freshness barrier signal fires. 15 new test pins: `tests/lex-transcript-ref-stale.test.ts` (6 `isRefStale` predicate rules + 2 `summarizeRefStaleness` aggregations) + `tests/lex-transcript-ref-latest-chunk-ms.test.ts` (3 write-path pins + 4 preload sync behavioural pins). 952/952 daemon tests pass (was 937); tsc clean. Rebuild: yes (07-daemon dist regen + migration 041 applies on next boot). |
| 41 | smart-compact policy refactor (daemon -> Lex) — SHIPPED ALL STAGES | ✅ | Stage 1 e936908 + Stage 2 bb12b4a + Stage 3 6359fd2, 2026-05-26 04:33 EDT | **Three-stage refactor per `docs/bugs/2026-05-26-smart-compact-policy-refactor.md`.** Daemon-side smart-compact policy fully removed; Lex owns every decision (threshold, window, stop-point classification, shadow gating, wrap-prompt authorship, fire/wait/wrap verdict, poll cadence). Stage 1 (e936908) added the three policy-out endpoints (`GET /state`, `POST /clear-and-paste`, `POST /wrap-paste`) + migration 040 widening `smart_compact_log.action` CHECK. Stage 2 (bb12b4a) extracted `lex/smart-compact-policy.ts` (`evaluateTriggerForAnchor` + `WRAP_AND_COMMIT_PROMPT`) and added the `smart_compact_policy_owner` runtime flag + scheduler short-circuit branch. Stage 3 (6359fd2) deleted the scheduler module, the `evaluate` route, the `evaluateSmartCompact` function, per-anchor `isShadow` gate, `shadowThreshold`, `defaults`. Default policy owner flipped to `'lex'`. Per investigation Section 3.3 shadow gating killed entirely. Daemon retains transport (PTY + bridge injector with auto-CR nudge), audit log writes, readiness gate (`awaitNewSessionReady`), and the operator-level `off|shadow|live` mode kill-switch. 937 daemon tests pass; tsc clean across all three commits. Rebuild: yes (07-daemon dist regen + migration 040 applies on next boot). |
| 41 Stage 2 | smart-compact policy module + scheduler short-circuit | ✅ | bb12b4a, 2026-05-26 04:22 EDT | **Stage 2 of three-stage Fix 41 refactor.** New module `07-daemon/src/lex/smart-compact-policy.ts` owns the decisioning surface. `evaluateTrigger` body + `WRAP_AND_COMMIT_PROMPT` moved here from `07-daemon/src/lex/smart-compact.ts`; legacy module keeps re-exports through the cutover, Stage 3 deletes them. New Lex-facing wrapper `evaluateTriggerForAnchor(state, defaults, opts)` takes the same shape `GET /lex/smart-compact/state` returns and produces `{action, reason, stop_point}` where `stop_point` classifies why fire/wait landed where it did (`recent_commit` / `idle_tool` / `idle_phase` / `none` / `n/a`) with priority `recent_commit > idle_tool > idle_phase`. Phase / EvalAction / EvalReason types hoisted to `07-daemon/src/lex/smart-compact-types.ts` to avoid a re-export cycle. New runtime_config flag `smart_compact_policy_owner` with values `'daemon' \| 'lex'`, default `'daemon'` this commit. New `GET` + `POST /lex/smart-compact/policy-owner` expose the toggle for the dashboard (flip without daemon restart). `runSmartCompactTick` reads the owner once per tick; when `'lex'` the scheduler walks anchors + runs evaluate (audit surface still records what the daemon's evaluator would have said) but skips every inject. New `TickResult.shortCircuited` lists anchors that were log-only. Wait verdicts still flow into `result.waited` so the dashboard's normal wait timeline is unaffected. `daemon.ts` log line gains `short_circuit=` count. Default stays `'daemon'` so Stage 2 is opt-in until Lex loop verified. 30 new test pins: 16 in `tests/smart-compact-policy.test.ts` (policyDefaults + legacy evaluateTrigger contract + every branch of evaluateTriggerForAnchor + stop-point classifier), 4 in `tests/smart-compact-scheduler.test.ts` (short-circuit wrap, short-circuit fire, wait still records, daemon owner back-compat), 10 in `tests/smart-compact-routes.test.ts` (parse/resolution helpers + GET/POST /policy-owner endpoint). 966/966 daemon tests pass (was 936); tsc clean. Rebuild: yes (07-daemon dist regen). |
| 41 Stage 1 | smart-compact policy-out endpoints | ✅ | e936908, 2026-05-26 04:15 EDT | **Stage 1 of three-stage Fix 41 refactor per `docs/bugs/2026-05-26-smart-compact-policy-refactor.md` Section 4.** Adds three new endpoints that split mechanical work out of the policy surface so Lex can drive the loop. `GET /lex/smart-compact/state` returns the consolidated raw inputs (`ctx_pct`, `last_commit_ms`, `last_tool_ms`, `jsonl_path`, `shadow_count`, `mode`) Lex needs to run `evaluateTrigger` locally — no decision, no inject, no audit row. `POST /lex/smart-compact/clear-and-paste` takes a Lex-authored summary; daemon runs `/clear` + the existing `awaitNewSessionReady` gate + summary paste + audit row with `action='clear-and-paste'`. `POST /lex/smart-compact/wrap-paste` takes a Lex-authored wrap prompt; daemon injects once + writes audit row with `action='wrap-paste'`, no daemon-side `WRAP_AND_COMMIT_PROMPT` default. All three enforce `mode='off'` short-circuit (noop, no audit row), 400 on empty required fields, 404 on unknown anchor. Existing `/evaluate` and `/fire` endpoints unchanged through Stage 1; Stage 2 wires the scheduler short-circuit on the `smart_compact_policy_owner` runtime flag and Stage 3 deletes the legacy surface entirely. Migration 040-smart-compact-action-extend.sql rebuilds `smart_compact_log` with the extended `action` CHECK so the two new action values pass; existing rows copy verbatim. `dashboard/routes.ts` wires the production ctxProvider (`deriveContextFromTail` -> percent) into `registerSmartCompactRoutes` so `/state` and the scheduler report identical numbers for the same jsonl tail. 27 new test pins in `tests/smart-compact-routes.test.ts` (5 readSmartCompactState, 3 GET /state, 5 clearAndPaste, 6 POST /clear-and-paste, 4 wrapPaste, 4 POST /wrap-paste) covering happy path + 400 + 404 + off-mode noop + readiness-gate async + audit-row-shape pins. 936/936 daemon tests pass (was 909); tsc clean. Rebuild: yes (07-daemon dist regen + migration 040 applies on next boot). |
| 40 | cc-pty voice double-talk: speak-queue serialises same-turn segments | ✅ | be14396, 2026-05-26 03:47 EDT | **Symptom:** operator-reported 2026-05-26 03:28 EDT live: cc-pty Lex CC session played overlapping TTS. Investigation doc `docs/bugs/2026-05-26-cc-pty-double-talk-investigation.md` Candidate 1: `speak()` at `lex-voice-ws.ts:1288` reassigned `state.ttsActive` without cancelling prior ctx. Multi-segment replies (pre-tool ack + end_turn body) spawned two piper children whose PCM streams both reached the client. **Contract (operator-clarified):** (a) same logical turn = SERIALIZE (second piper waits for first's natural end, no overlap, no cut); (b) across turn boundary = CANCEL (in-flight ctx cancelled, partialChain captures intended_text); (c) same-turn natural completion does NOT push a partialChain entry. **Fix:** new pure module `07-daemon/src/voice/lex-voice-speak-controller.ts` exports `createSpeakController(state, deps)`. Owns piper lifecycle: `speak()` pushes onto `state.ttsQueue`, `runQueue()` drains one at a time awaiting each handle's natural end, `killActive()` cancels in-flight ctx + clears queue + records partialChain for in-flight intended_text only. `ConnState` gains `ttsQueue` + `ttsQueueRunning`. `speak()` in `lex-voice-ws.ts` is now a thin delegating wrapper. `killActiveTts` + `runHoldUp.cancelTts` both delegate to `speakCtrl.killActive()` so all barge paths stay in lock-step. Investigation Candidate 3 (multi-assistant-record turns beyond pre-tool ack) is covered by the same fix. Candidate 5 (cross-tab race) deferred. **Tests:** 909/909 daemon tests pass (was 903); tsc clean. Six new pins in `tests/lex-voice-ws-speak-queue.test.ts`: (1) two same-turn speaks → sequential spawns, ctx1 not cancelled, partialChain empty; (2) killActive mid-queue → in-flight cancelled, queue dropped, partialChain has only the in-flight entry; (3) speak after killActive → fresh ctx immediately; (4) speak/kill/speak → no overlap, partialChain has only the cancelled A; (5) killActive returns false on idle barge; (6) killActive clears orphaned queue with no in-flight ctx. Rebuild: yes (daemon TS, lex-voice-ws.ts changed). |
| 39 | Dashboard grooming: drop dead Start-skip-permissions button | ✅ | 9e39e45, 2026-05-26 03:30 EDT | UI dead-code removal companion to Fix 37. The dashboard's "Start (skip permissions)" button in `08-dashboard/components/SessionsTable.tsx` was wired to flip `dangerous=true` on `/projects/:id/start-claude`; the daemon now ignores that flag and emits a warning. Removed the button so the UI stops suggesting an affordance that no longer exists. `pendingStart` state collapses from `Record<string, "vanilla" | "skip">` to `Set<projectId>`. `startMutation` takes a plain project id instead of the (id, dangerous, mode) tuple. `lib/daemon-client.ts` `startClaude(projectId)` signature drops the `dangerous` arg; body still sends `{dangerous: false}` so daemon endpoint shape stays stable. Dashboard tsc clean. Effect covered by the daemon-side pin in `tests/projects-routes.test.ts` (Fix 37). Rebuild: yes dashboard (Next.js static export); daemon unchanged. |
| 38 | Docs sweep: smoke cursors + bug logs rolled forward | ✅ | c899113, 2026-05-26 03:25 EDT | Bookkeeping commit. Rolling-cursor smoke docs caught up after tonight's commit cascade on the supervisor wire (34d.1), false-shipment detector (34d.2), coalesce queue Phase A (35), LEX-AUTONOMY Stage 5 (36), and worker skip-permissions removal (37). Files updated: `docs/SMOKE-PROGRESS.md` (step 3.7 PASS, cursor rolls to Step 4 repoint), `docs/SMOKE-HANDOVER.md` (paste-ready resume regenerated), `docs/SMOKE-TEST.md` (checkmarks for Steps 0-3 fully green), `docs/bugs/2026-05-16-voice-restart-oom-regression.md` (Fix 27 recurrence section closed with vad-error logging evidence), `docs/bugs/2026-05-22-tts-bargein-regression.md` (Fix 17 + Fix 20 evidence rolled forward, pending soak), `TODO.md` (operator queue line edits). No code change; no rebuild needed. |
| 37 | Workers no longer accept --dangerously-skip-permissions | ✅ | b8c9021, 2026-05-26 03:22 EDT | **Architectural rule (operator, 2026-05-26):** workers must not bypass the permissions onboarding wizard. The permissions prompt is the only thing standing between an autonomous loop and a destructive shell call inside a user project tree. **Fix:** `openProjectAnchor` in `07-daemon/src/dashboard/projects-routes.ts` always emits `command='claude'` (the `dangerous` option becomes inert for project anchors). When a legacy caller still ships `dangerous=true`, the daemon surfaces a warning in `res.warnings`: `"skip-permissions ignored: workers no longer accept --dangerously-skip-permissions per architectural rule (2026-05-26)"`. Lex's own brainstorm spawn (`/spawn-lex` + `/lex/anchors/:id/open`) is unaffected; those paths pass `extraArgs:['--dangerously-skip-permissions']` explicitly because Lex runs in a daemon-owned scratch cwd with no project files, and without skip-permissions the wizard blocks the seed greeting and the SessionStart hook. **Tests:** 903/903 daemon tests pass; tsc clean. Existing pin `spawn path: dangerous=true emits --dangerously-skip-permissions` flipped to `spawn path: dangerous=true is INERT for workers; command is plain 'claude' + warning` — directly inverts the prior contract. Rebuild: yes (daemon TS, projects-routes.ts changed). |
| 36 | LEX-AUTONOMY Stage 5: anchor distillation pulled oldest chunks instead of newest | ✅ | 70dfd74, 2026-05-26 03:18 EDT | **Symptom:** anchor-level rolling distillation (`createLlmDistillationGenerator` in `07-daemon/src/lex/distillation-generator.ts`) called `db.listBrainstormChunks(row.id, chunkLimit)` with no `opts`. `listBrainstormChunks` defaults to ASC order, so on any active brainstorm with more than `chunkLimit` (default 50) chunks the generator summarised the OLDEST 50 turns globally; the tail of the conversation (where load-bearing decisions live) was silently dropped. Codex flagged as item 4 in `docs/spec/LEX-AUTONOMY-PAYLOAD-SPEC.md` order-of-operations. **Fix:** `buildTranscript` now fetches DESC then reverses to chronological for the prompt. `maxBytes` cap moved from "head of string" to "tail of string" so when the joined transcript exceeds the byte budget we keep the most recent content instead of dropping it. `createPerSessionDistillationGenerator` already handled its own DESC+reverse pattern; only the anchor-level path was wrong. **Tests:** 903/903 daemon tests pass; tsc clean. New regression pin in `tests/distillation-generator.test.ts` seeds 100 chunks and asserts the captured user prompt contains turn-99 + turn-50, does NOT contain turn-0 / turn-49, and that turn-50 appears before turn-99 (chronological order inside the slice). Rebuild: yes (daemon TS, distillation-generator.ts changed). |
| 35 | Coalesce utterance queue Phase A (single-output-stream invariant, contradiction-cancel) | ✅ | 2718b85, 2026-05-26 03:15 EDT | **Symptom:** direct-llm voice path had no reentrancy guard. A second utterance landing while `handleDirectLlmUtterance` was mid-ollama spawned a parallel ollama call and a concurrent `speak()`. Violated coalesce-contract point 1 (single output stream). Sealed contract: `docs/spec/COALESCE-UTTERANCE-QUEUE.md`. **Fix:** new pure module `07-daemon/src/voice/lex-voice-coalesce.ts` with `formatQueueDrain(queue)` (single-item passthrough or `[voice-context: queued-utterances (N)]` preamble + numbered list) and `detectContradiction(text)` (word-bounded regex lexicon: cancel / never mind / forget it / stop|halt|abort / drop it / hold up|on). `ConnState` gains `inFlightDirectLlmReply`. The direct-llm branch in `handleUtteranceEnd` now gates: mid-reply + contradiction → drop queue + emit `t:'contradiction-cancel'`; mid-reply + non-contradiction → push onto `pendingUserUtterances` + emit `t:'queued-mid-turn'`; no reply in flight → dispatch through `runDirectLlmCoalesceLoop` which sequences `handleDirectLlmUtterance` calls and drains the queue between iterations via `formatQueueDrain`. cc-pty path keeps its existing mid-turn queue. **Phase B / C deferred:** code-side classifier (relevance / follow-up / new / noise tagging), conflict push-back against memory rules, AbortController plumbing into `callVoiceChat`, and the text-input WS frame (depends on dashboard chat UI). Tracked in the spec. **Tests:** 902/902 daemon tests pass; tsc clean. Eight pins in `tests/lex-voice-coalesce.test.ts` cover formatter shape (empty, single-item passthrough, multi-item preamble, order preservation) + detector behavior (canonical phrasings, case-insensitive, word-boundary false-fire prevention, empty input). Rebuild: yes (daemon TS, lex-voice-ws.ts changed). |
| 34d.2 | Narrated-success-no-commit detector (false-shipment guard) | ✅ | 3766190, 2026-05-26 03:10 EDT | **Symptom:** tonight worker assistant narrated "Bundle shipped" for Fix 34d.1 work that did not advance git HEAD. Cron-driven Lex supervision had no detector that compared narration against git, so it trusted the words. **Fix:** new WorkerEventType `narrated_success_no_commit` + new pure module `07-daemon/src/dashboard/worker-event-git.ts` (cached `git rev-parse HEAD` + `git log -n3 --pretty=%h %s` per anchor cwd, 5 s TTL, `windowsHide:true` per operator rule). `AnchorTailState` gains `pendingSuccessClaim { text, ts, headShaAtClaim, fired }`. Three-step state machine in `deriveEvents`: (1) observe newest assistant claim matching `/\b(?:shipped\|landed\|completed?\|done\|ready\|deployed\|merged)\b/i` (skips pre-tool acks where `stop_reason='tool_use'`); seed `pendingSuccessClaim` with current HEAD when newer than tracked. (2) Clear if HEAD advanced (commit landed for that claim). (3) Fire once the grace window (default 60 s) elapses with HEAD unchanged; latch `fired=true` so a chatty worker cannot re-fire on the same tail. Snippet formatter: `claim: <text>\ngit HEAD did not advance\nsha_at_claim: <12-char prefix>\nrecent_commits:\n- <sha> <subject>` (last 3). `worker-event-listener.ts` `processChange` reads HEAD + recent commits via `defaultGitHelpers` and threads into `deriveEvents` opts. `ListenerDeps` + `ProcessChangeDeps` gain `gitHelpers` test seam + `successClaimGraceMs` override. Rate limit: shared `WorkerEventGate` `perTypeMinGapMs` default 5 min caps this event at 1 fire per 5 minutes per anchor. **Tests:** 894/894 daemon tests pass; tsc clean. Three pins in `tests/worker-event-detect.test.ts`: (a) claim + no-commit + grace elapsed = fires + snippet shape; (b) claim + HEAD advanced = no fire + pending claim cleared; (c) per-claim tracking — earlier claim with landed commit does not retroactively fire when a fresh claim arrives at the post-commit HEAD. Two pins in `tests/worker-event-snippet.test.ts`: formatter output + missing-context fallback. Rebuild: yes (daemon TS, worker-event-listener.ts changed). |
| 34d.1 | Supervisor wire routed to worker terminal, not Lex (Fix 34d aftermath) + raw-tail-bytes snippet was pure noise | ✅ smoke-verified 2026-05-26 03:15 EDT | (this commit, 2026-05-26 02:55 EDT) | **Two bundled fixes** — routing + snippet picker — shipped in one daemon rebuild. **(A) Routing bug. Symptom:** event-driven supervisor inject payloads (`[supervisor-event] worker=… event=idle…`) landed as queue-operation rows in the WORKER's CC jsonl instead of Lex's. Audit row claimed `decision='accepted'` `reject_reason='delivery_mode=lex-queue'`. Operator caught the misroute 2026-05-26 02:30 EDT. **Root cause:** `deliverSupervisorPromptToLex` (Fix 34d) called `queueSessionPrompt(ref.lex_session_id, text)`. `queueSessionPrompt` writes to `<DATA_ROOT>/bridge-prompts/<sessionId>.in`, which the 09-bridge VSIX watches and pastes into the matching terminal. The `<id>.in` shape requires a CC session id; `ref.lex_session_id` is a brainstorm/lex_session row UUID. The bridge VSIX with no matching terminal silently dropped into whichever VS Code terminal was in scope = the worker. **Architectural rule re-affirmed by operator 2026-05-26 02:35 EDT:** daemon NEVER injects directly to worker. Daemon notifies Lex; Lex decides. Only Lex's outputs reach worker. **Fix:** replace the lex-queue branch with `ptyInject(lexCcSessionId, text, true)` from `07-daemon/src/dashboard/pty-host.ts`. `ptyInject` resolves by `ptyId` first then by `sessionId`; Lex CC runs under a daemon-managed pty so the cc id matches `handle.sessionId` and the write lands at Lex's terminal directly (no bridge round-trip). The trailing CR commit + 1 s bare-CR follow-up nudge are baked into `ptyInject` already, so no extra plumbing needed. `deliverSupervisorPromptToLex(db, lexCcSessionId, text, injectPty?)` signature: third param is now an `injectPty` test seam typed `(ccId, text, commit) => { ok: true } | { ok: false; error }`. Mode label flips from `lex-queue` to `lex-pty`. `buildInject` also tightens the non-Lex-target branch: instead of falling through to `crossSessionInject` (which could deliver to a worker via bridge/pty), it now drops with `rejected-not-lex / target_not_lex_cc` and audits accordingly. Supervisor wire is Lex-only by construction. Dropped imports: `crossSessionInject`, `issueToken`, `queueSessionPrompt`. New import: `ptyInject`. **Tests:** four pins in `07-daemon/tests/worker-event-listener.test.ts`: (a) `no_lex_transcript_ref` when target cc not bound to a Lex session (unchanged shape, mode='lex-pty'); (b) routes via injectPty keyed on the CC id (NOT lex_session.id) — directly inverts the pre-fix bug; (c) injectPty error surfaces as `lex-pty` failure; (d) contract pin via `vi.spyOn(sessionsModule, 'queueSessionPrompt')` asserting the bridge writer is NEVER called under any branch — any future regression that wires the bridge fallback back in will fail loudly. **(B) Snippet picker. Symptom (bundled with the routing bug):** when supervisor payloads did arrive at any terminal, the `Snippet:` block was the last ~2 KB of raw jsonl bytes — usually CC's SessionStart skill-catalog (`"skillCount":99,"isInitial":true`), a `hook_additional_context` attachment, or whatever queue-operation envelope the worker had just written. Pure noise; Lex had no actionable signal. **Fix:** new pure module `07-daemon/src/dashboard/worker-event-snippet.ts` exports `extractEventSnippet(eventType, rawTail, opts)`. Walks the tail with the same meaningful-line predicate `brainstorm-jsonl-ingestor` uses (skip `isMeta` / `isCompactSummary` / `attachment` records; keep only `user` and `assistant` records with text + tool_use + tool_result parts). Per-event formatter: **idle** → `stall_seconds=<N>` + `last_user` + `last_assistant` (skips pre-tool acks where `stop_reason='tool_use'`) + `last_tool: <name>(<input>)` + `last_tool_result: [ok\|err] <text>`; **permission_denied** → `denied_tool` + `denied_input` (resolved from matching `tool_use_id`) + `reason` from the canonical phrase line; **commit** → `branch` + `subject` + `files_changed` parsed from the `[branch hash] subject` line and the `N files changed` footer; **expectation_drift** → `drift: <supervisor-provided summary>` (jsonl tail is not used; the supervisor passes the LLM-judged drift summary via `driftSnippet`); **test_failure / pending_prompt / bridge_disconnect** → fallback `last_user` + `last_assistant`. Output capped at 600 chars total with middle truncation marker `... [truncated N chars] ...`; empty case returns `(no recent activity)` — never silently swallows an event. `worker-event-detect.ts` `pushIfFireable` now stamps `snippet: extractEventSnippet(type, parsed.snippet, { now })` instead of raw bytes. **Tests:** new `07-daemon/tests/worker-event-snippet.test.ts` with 12 pins covering parseMeaningfulLines filtering (attachment / isMeta / isCompactSummary), per-event-type formatter outputs, empty-case fallback, 600-char cap, and the explicit `last_assistant` skip of pre-tool acks. Existing `worker-event-detect.test.ts` permission_denied pin updated from `/Permission to use Bash/` to `/denied_tool:\s*Bash/` to reflect the new output shape. **Combined test totals:** 889/889 daemon tests pass; tsc clean. **Live verification (post-rebuild + restart):** wait for worker idle, observe (1) supervisor inject row in `cross_session_injection_log` with `reject_reason='delivery_mode=lex-pty'`, (2) `[supervisor-event]` text appears in Lex CC jsonl 2a708d6d (not worker), (3) the `Snippet:` block contains structured `stall_seconds=…\nlast_user: …\nlast_assistant: …` (NOT raw jsonl bytes or skill catalog), (4) worker jsonl has NO new queue-operation rows containing `[supervisor-event]`. Bug doc: `docs/bugs/2026-05-26-supervisor-wire-routes-to-worker.md`. Rebuild: yes daemon TS (worker-event-listener.ts + worker-event-snippet.ts + worker-event-detect.ts + tests). |
| 34b | chokidar v4 glob-string watch never matched any jsonl file | ✅ | (this commit) | **Symptom:** post-Fix-34 restart at 11:51 PM EDT 2026-05-25, `/dashboard/health-supervisor` stayed on `health='no-events'` despite real worker writes (session 837dd156 git commit f9555ce at 12:43 AM EDT). `chokidar.bound` counter ticked to 1, `chokidar.line` stayed at 0 forever. Zero rows in `cross_session_injection_log` with `caller_label='event-supervisor'`. **Root cause:** chokidar v4 removed glob support entirely. `worker-event-listener.ts` called `chokidar.watch(`<root>/<doublestar>/<asterisk>.jsonl`, ...)`. In v4 that string is treated as a LITERAL file path; since no file with that exact name exists, the watcher binds (emits 'ready', which the diagnostics module counts as `chokidar.bound`) but never matches any real file. Every subsequent jsonl write is silently ignored. The companion fs-watcher.ts already uses the v4-correct form `chokidar.watch(root, { ignored: ... })`; worker-event-listener.ts was the only remaining glob caller. **Fix:** swap the glob string for the directory `root` + an `ignored` predicate that skips non-jsonl files: `(filePath, stats) => stats?.isFile() ? !filePath.endsWith('.jsonl') : false`. Directories pass through (chokidar recurses), .jsonl files fire 'add' / 'change', everything else is dropped before the handler. Also passed `ignorePermissionErrors: true` to mirror fs-watcher's defensive setting; ~/.claude/projects can contain unreadable subdirs on Windows in rare cases. 36/36 worker-event-listener + worker-event-router tests pass; ignored-predicate signature is identical to the form already validated by fs-watcher in production. **Verification:** static. Operator restart → write to any worker jsonl → expect `chokidar.line` counter > 0 within seconds and `health='no-route'/'no-inject'/'ok'` depending on downstream readiness. Rebuild: yes daemon TS — chokidar watch arg shape changed. |
| 34 | Event-driven Lex supervisor delivered zero rows to cross_session_injection_log | ✅ | 9b2bebf (instrumentation) + 226fe67 (resolver fix); chokidar v4 glob breakage continued in Fix 34b | **Symptom:** anchor `391b88f6` was `supervision_mode='event'` and the listener was wired in `daemon.ts:201`, but no rows ever landed in `cross_session_injection_log` from the supervisor path. The wire looked alive (chokidar bound, anchors in event mode) but every event silently no-op'd downstream. **Instrumentation (commit 9b2bebf):** migration 039 + `worker-event-diagnostic-log` table + `worker-event-diagnostics.ts` writer with stages `chokidar.bound`, `chokidar.line`, `detector.matched`, `gate.evaluated`, `route.resolved`, `inject.attempted`, `inject.result`. Verbose stages gated behind `DEVNEURAL_SUPERVISOR_DEBUG=1`; the minimal trio (route.resolved / inject.attempted / inject.result) stays on in production so a future regression surfaces in `/dashboard/health-supervisor`. In-process counters are unconditional regardless of the debug gate. Two new routes: `GET /dashboard/worker-event-stats` (boot ms, per-stage counters, recent 20 rows, derived health verdict) and `GET /dashboard/health-supervisor` (coarse `no-binds`/`no-events`/`no-route`/`no-inject`/`inject-failed`/`ok` verdict). **Root cause:** `resolveLexTargetSession` in `worker-event-router.ts` did not check `lex_session.supervises_project_anchor_id`. It picked the most-recently-created live `lex_session` GLOBALLY, regardless of which project the worker event belonged to. When more than one live `lex_session` existed and the project's true supervisor was not the most-recent live row, the resolver returned the wrong session id OR returned null and the inject path bailed at `outcome: 'no-target'`. Compounding factor: the resolver kept a single 60s null-result cache, so the first null verdict at boot poisoned every subsequent lookup for a full minute. **Fix:** new `db.listLexSessionsBySupervises(projectAnchorId, opts)` that filters by `supervises_project_anchor_id` AND `status` in one SQL call. `resolveLexTargetSession` gains an `anchorId` option; when set, it tries the supervises-bound path FIRST and only falls back to global-pick when no Lex session has explicitly bound the project. Cache is now keyed per-anchor (`Map<cacheKey, CachedTarget>` instead of one global slot) so a null verdict for project A does not block project B. `ProcessChangeDeps.resolveTarget` becomes optional; the listener no longer installs a global-pick default (which silently shadowed the anchor-scoped lookup pre-fix), and `processChange` calls `resolveLexTargetSession(deps.db, { anchorId: anchor.id })` directly when no override is supplied. Three new contract tests pin: (a) supervises-bound wins over more-recent unbound, (b) global fallback when no supervisor exists, (c) per-anchor cache isolation. 64/64 worker-event + cross-session-inject tests pass. **Verification:** static analysis only this cycle; live end-to-end synthetic-event probe deferred to the next operator session. The instrumentation is now in place so the operator can curl `/dashboard/worker-event-stats` post-restart to confirm the wire delivers. Rebuild: yes daemon TS + new diagnostic table migration. |
| 26 | "Lex hold up" wake-phrase kills mic input permanently | 🔍 diagnosed | — | **Symptom:** "Lex hold up" mid-Lex-speech, Lex pauses + replies recap correctly, after that mic is dead; voice off/on does NOT recover, only full page refresh + voice restart. **Root cause (asymmetric cancel path):** `hold_up` dispatch in `07-daemon/src/voice/lex-voice-ws.ts:1498-1551` runs a `cancelTts` closure that mutates server state (sets `state.ttsActive=null`, kills piper, pushes partialChain) **but does NOT send `{t:'tts-cancel'}` to the client.** The original `killActiveTts` at `07-daemon/src/voice/lex-voice-ws.ts:1275` DOES send tts-cancel as step 2 of its three-step teardown. Client `case "tts-cancel"` at `08-dashboard/components/VoiceClient.tsx:2033-2048` calls `resetTtsPlayback()` which is the canonical playback teardown: bumps `ttsGenRef` so in-flight chunks drop, hard-stops every scheduled AudioBufferSourceNode, sets `speakingRef=false`, `ttsActiveRef=false`, `micGatedRef=false`. Without that frame the client's `speakingRef`/`ttsActiveRef`/`micGatedRef` stay true. Then hold_up's `runHoldUp` sends `voice-hold-up` (no client case, falls through silently) + `voice-listen` (`setMicMuted(false)`) + `speak(recap)`. The recap's tts-start at line 1944 re-sets the same flags (they were already true). Recap audio plays. **The bug surfaces if any of these happen between cancelled TTS and recap completion:** (1) Client receives a late binary PCM chunk from the cancelled stream — `schedulePcmChunk` line 1110 does not gen-drop (gen wasn't bumped), so it schedules into the now-uncoordinated activeSourcesRef. (2) Recap's tts-end fires `finalizePlaybackEnd` at line 913 BUT the function requires `streamFinishedRef.current` (line 914); the cancelled TTS never sent tts-end so the prior `streamFinishedRef=false` from the cancelled cycle persists, then recap tts-start at line 1976 resets to false, recap tts-end sets to true. **Actual breakage point:** `finalizePlaybackEnd` at line 924 calls `vadRef.current?.start?.()`. If silero VAD's internal worklet got into an inconsistent state during the cancel-without-cleanup window (no `vad.pause()` was called, no `vad.start()` is technically needed — but vad.start() on an already-running vad can be a no-op or throw depending on @ricky0123/vad-web version), the restart may silently fail. The user's mic LOOKS dead because silero isn't firing onSpeechStart anymore. **Why voice off/on doesn't recover:** the [enabled, sessionId, mode] effect cleanup tears down WS + VAD + mic stream but the daemon-side `state.partialChain` accumulates (Lex's interrupted-reply context). Next user voice-on reconnects, hello-ack lands, VAD inits fresh. But if the previous cycle left silero's WASM env in a weird state (`getVadModule` cache mentioned at line 1559-1568), the new MicVAD.new() may instantiate against a half-disposed ORT backend. **Recommended fix (one of):** (1) **Add `send({t:'tts-cancel', reason:'hold-up'})` to the hold_up cancelTts closure at lex-voice-ws.ts:1511-1527** — mirrors killActiveTts's three-step pattern; client gets resetTtsPlayback for free; cleanest. (2) **Add a client-side `case "voice-hold-up"` handler in VoiceClient.tsx:2033-2048 area that calls `resetTtsPlayback()`** — belt-and-suspenders; works without daemon change. (3) Combine both for defense-in-depth. **Operator-reported "voice off/on doesn't recover" symptom suggests the silero/ORT state corruption is real and may need additional `resetVadModuleCache()` invocation tracking** (currently called in teardown at line 1568 only). Investigation only; no fix shipped this cycle. |

> **Note: no Fix 8.** The slot was skipped intentionally between
> Fix 7 and Fix 9; the original Fix 8 placeholder did not earn its
> number and was never specced. The numbering gap is preserved on
> purpose so the per-fix commit subjects stay traceable.

## Smoke test (pending)

Source of truth for the live checklist: `docs/SMOKE-TEST.md` (ACTIVE BATCH 2026-05-25). Rolling cursor: `docs/SMOKE-PROGRESS.md`. Paste-ready resume: `docs/SMOKE-HANDOVER.md`.

Bundled commits awaiting one daemon rebuild + restart before any verification step runs (cumulative since last smoke pause):

| Commit | Fix | What it changes | Verify how |
|---|---|---|---|
| `862d42a` | 29 | ref_summary write on direct-llm fallback + SSE `brainstorm-ended` broadcast | Step 3.1-3.4 in `SMOKE-TEST.md` (tail `daemon.stdout.log` for `[chunks-fallback] ref_summary written`; query `lex_transcript_ref.ref_summary` non-null; dashboard tile flips to ended within ~2s without manual refresh) |
| `6e37c07` | 32 | mid-turn flush `pendingUserUtterances` gains explicit 850 ms bare-CR follow-up | Open brainstorm; speak mid-turn while Lex is still responding; queued utterance must auto-submit after the turn boundary (no manual Enter required) |
| `b3b30f4` | 33 | cancelled-tool-recovery primitive wired in daemon.ts; 5 s arm + 30 s strike window + two-strike escalation | Force a tool-result reject in Lex's session (e.g. mid-tool user message); within ~5 s a `[recovery] A tool call was cancelled` row should land in `cross_session_injection_log` with `caller_label='lex-cancelled-tool-recovery'` and Lex should auto-resume the next turn |
| `9b2bebf` + `226fe67` | 34 | event-driven supervisor instrumentation (`worker_event_diagnostic_log`, `GET /dashboard/worker-event-stats`, `GET /dashboard/health-supervisor`) + `resolveLexTargetSession` now picks the project-bound Lex anchor first | `curl /dashboard/health-supervisor` returns `health: 'ok'` after one worker event lands; new row in `cross_session_injection_log` with `caller_label='event-supervisor'` after worker emits a commit or other watched event |

Run order:

1. `cd 07-daemon && npm run build` — daemon dist regen.
2. `cd 08-dashboard && npm run build` — dashboard static export regen.
3. Restart the daemon (dashboard restart-daemon button OR kill + relaunch); confirm `/health` returns `ok=true` with new pid.
4. Resume from the current cursor in `docs/SMOKE-PROGRESS.md`. Today's cursor is Step 3.1.
5. After Step 3.x passes, run the four post-restart probes above for Fix 32, 33, 34. They are independent of Step 4-5 of the active batch and can run in any order.

Do NOT delete this file until every row in the table above has flipped to ✅ smoke-verified with timestamp on its FIXES row.

## Cleanup

Delete this file once the smoke test passes.
